# ✈️ AI 旅行行程规划师（H5）

一个「**只说需求，一键生成可落地行程**」的旅行规划应用。支持**对话式规划**与**手动表单**两种方式：对话模式下用自然语言描述需求（如"带爸妈去杭州玩3天，预算适中，想逛西湖吃小吃"），本地 AI 模型自动抽取目的地/天数/人群/预算/风格等参数并生成行程；手动模式逐项设置参数。AI Agent 结合**高德地图地理数据**（POI 检索、路径耗时、天气）编排逐日行程，输出结构化 JSON，前端以移动端 H5 展示并支持**地图可视化 + 拖拽重排自动重算路线**。

## 技术栈

> 运行效果预览见 `docs_app_preview.png`。

| 层 | 选型 |
|---|---|
| 前端 | Vue 3 + Vite（H5 移动端） + Vue Router |
| 后端 | Python FastAPI + Pydantic |
| AI 模型 | 本地轻量模型，经 **Ollama** 运行（默认 `qwen2.5:3b`，自动降级到本机已有模型，如 `qwen2:7b`） |
| 地图 | 高德地图 Web 服务 API（后端数据）+ 高德 JS API 2.0（前端渲染，可选） |

> AI 只负责**编排、取舍、文案、避坑提示**；坐标、距离、移动耗时、天气等地理事实全部由高德 API 返回，杜绝大模型"脑补车程"式幻觉。

## 目录结构

```
travel/
├── backend/                  # Python FastAPI 后端
│   ├── app/
│   │   ├── main.py           # 路由入口
│   │   ├── config.py         # 环境配置
│   │   ├── models.py         # Pydantic 请求/响应模型
│   │   ├── amap.py           # 高德地图客户端（geocode/POI/路径/天气，含 mock 兜底）
│   │   ├── ai.py             # Ollama 本地模型接入 + 旅行规划系统提示词
│   │   ├── intent.py         # 对话意图解析（AI 优先 / 规则兜底）
│   │   └── planner.py        # 行程规划引擎（AI 优先 / 规则引擎兜底 / 时间轴校验）
│   ├── requirements.txt
│   └── .env.example
└── frontend/                 # Vue3 + Vite H5
    ├── src/
    │   ├── views/Home.vue    # 需求输入页（对话规划 / 手动填写双模式 + 周边探索入口）
    │   ├── views/Plan.vue    # 行程结果页（地图 + 预算 + 天数 Tab + 拖拽重排）
    │   ├── views/Explore.vue # 周边探索页（地图选点 → 放射状展示周边可去点位）
    │   ├── components/       # DayCard（单日行程）、MapView（高德/SVG 双模式）、ChatPlanner（对话规划）
    │   ├── api/              # 后端接口封装
    │   └── constants.js
    └── .env.example
```

## 快速启动

### 0. 依赖

- Node.js ≥ 18
- Python ≥ 3.10

### 1. 启动后端（端口 8000）

```bash
cd backend
python3 -m venv .venv
.venv/bin/pip install -r requirements.txt
cp .env.example .env        # 按需填入 AMAP_KEY
.venv/bin/python run.py
```

接口文档：http://127.0.0.1:8000/docs

### 2. 启动前端（端口 5173）

```bash
cd frontend
npm install
cp .env.example .env        # 按需填入 VITE_AMAP_WEB_KEY
npm run dev
```

浏览器打开 **http://127.0.0.1:5173** 即可使用（前端已代理 `/api` 到后端）。

### 3.（可选）启用本地 AI 模型

```bash
# 安装 Ollama：https://ollama.com/download
# 拉取轻量模型（约 2GB，8GB 内存可流畅运行）
ollama pull qwen2.5:3b

# 后端会自动探测：配置的模型未拉取时，自动使用本机已有模型
```

> 未启动 Ollama / 未安装模型时，应用自动降级为**内置规则引擎**，同样可以生成完整行程（带真实路线耗时），开箱即用。

### 4.（推荐）配置高德地图 Key

| 用途 | 类型 | 配置位置 |
|---|---|---|
| 后端真实数据（POI/路径/天气） | **Web服务** Key | `backend/.env` → `AMAP_KEY=` |
| 前端真实地图渲染 | **Web端(JS API)** Key | `frontend/.env` → `VITE_AMAP_WEB_KEY=` |

申请地址：https://console.amap.com/dev/key/app

> 未配置 Key：后端用内置演示数据（覆盖杭州/北京/上海/成都/西安/广州等热门城市 + 通用点位），前端用 SVG 示意图，全流程可跑通；配置后自动切换为真实数据与真实地图。
>
> 内置 `backend/app/data/place_geo.json`：全国 363 个地级市 + 34 个省级 + 3147 个区县归属 + 112 个精确区县坐标。即使未配置 Web服务 Key，任意中文地名（如"茌平区""喀什""拉萨"）也能定位到正确区域，而不是默认落在北京。

## 核心功能

- **💬 对话式规划**：首页切换到"对话规划"，用自然语言描述需求（支持多轮调整，如"改成4天""预算经济一点"）。本地 AI 模型自动抽取参数，缺目的地时主动追问；生成后一键进入行程页。
- **🧭 周边探索**：地图上任选一个位置作为中心点，以放射状可视化展示周边可去点位（可选 3/5/10/20km 半径、景点/美食/购物/夜生活类别），底部列表含评分/距离/区域，点击聚焦。
- **AI 一键生成行程**：目的地、天数、人群（含亲子/老人节奏适配）、预算档位、旅行风格、**交通方式（自驾/公共交通/混合）**、每日起止时间、兴趣标签、必去/排除点位。
- **Agent 工具调用**：高德 POI 检索 → 大模型编排 → 后端按真实路线耗时填充时间轴 → **时间轴合法性校验**（活动按"路上耗时+停留"累加，超出每日窗口自动截断）。
- **天数补齐**：AI 偶发只生成部分天数时，规则引擎自动补齐剩余天数，保证与用户请求一致。
- **地图可视化**：行程页地图展示逐日路线与点位（高德 JS API 或 SVG 示意图）。
- **拖拽重排**：拖动当日点位自动重算站间交通耗时。
- **预算拆解**：餐饮/门票/交通/住宿四项估算。
- **逐日 Tab + 天气贴士 + 避坑提示**。

## 项目进展 / 变更记录

> **约定**：每一次功能迭代、Bug 修复、性能优化都会在此**追加**记录（最新在上）。改动后必须同步更新本表（日期、类型、内容、涉及文件）。此约定已固化到 `AGENTS.md`，供后续开发/Agent 遵守。

| 日期 | 类型 | 改动内容 | 涉及文件 |
|---|---|---|---|
| 2026-09-03 | 项目开源准备 | **README 重构与开源准备**——1.将原 README.md（156KB，含完整变更记录和早期项目介绍）重命名为 CHANGELOG.md，保留全部开发历史；2.创建全新 README.md（28KB，541行），用于 GitHub 开源项目介绍，包含：项目简介、6大核心特性（智能规划/精细点位体系/人文知识体系/用户系统/管理后台/在线数据增强）、技术栈（后端 FastAPI+MySQL+DeepSeek+Chroma、前端 Vue3 用户端+管理后台）、完整项目结构（后端 8 层架构+前端双项目）、快速开始（7步环境搭建）、Docker 部署、配置说明（核心环境变量表）、API 文档、10阶段规划引擎核心流程图、文档索引、贡献指南、MIT 许可证；3.创建 LICENSE 文件（MIT 许可证）；4.更新 AGENTS.md 变更记录约定（从 README.md 迁移到 CHANGELOG.md）。新 README 内容已与项目当前状态同步（deepseek 线上模型、腾讯地图、邮箱验证码登录、RAG 知识库、管理后台 PC 端等），删除了已过时的信息（H5、手动表单、本地 Ollama、高德主地图等） | 重命名 `README.md`→`CHANGELOG.md`、新增 `README.md`（28KB）、新增 `LICENSE`（MIT）、修改 `AGENTS.md`（变更记录约定更新） |
| 2026-09-03 | 文档输出 | **输出RAG知识库增强技术实现方案文档（飞书文档）**——从技术开发角度详细说明RAG知识库增强产品的完整技术实现方案，文档包含13个章节：1.技术架构设计（整体架构图+架构特点+技术栈总览14项技术选型）；2.系统模块划分（后端7层模块结构+前端用户端/管理后台模块结构）；3.核心技术选型（LLM模型DeepSeek+Qwen备选、向量数据库Chroma→Milvus演进、后端框架FastAPI、前端框架Vue3的详细对比和选型理由）；4.数据库设计（MySQL 15+核心表详细字段设计含用户/行程/知识库/审核/对话5大类、Redis 10类Key设计、Chroma 6个集合设计+向量元数据结构）；5.API接口设计（8大模块60+API端点详细列表+统一响应格式+错误码设计40+错误码）；6.RAG实现方案（6步完整流程：查询理解→混合检索→重排序→上下文组装→增强生成→输出处理，含提示词示例、检索参数、缓存策略）；7.知识库构建方案（6大分类+数据来源8级优先级+8步采集流程+数据清洗4大规则+文本分段方案+向量化方案+数据更新策略）；8.性能优化方案（9项性能目标+后端4大优化+前端3大优化+RAG 3大优化）；9.安全方案（认证授权JWT+RBAC 4角色+数据安全+接口安全5维限流+运维安全）；10.部署方案（部署架构图+Docker Compose开发环境+生产蓝绿部署+服务器配置建议+CI/CD 6步流程+监控方案4大类指标）；11.开发计划和里程碑（6阶段12周详细计划+6个里程碑交付物和验收标准+团队配置8.5人）；12.风险和应对（技术风险6项+数据风险5项+业务风险5项+运营风险4项，每项含影响/概率/应对措施）；13.总结（技术方案7大核心特点+6大关键成功因素+5步下一步行动） | 飞书文档：https://my.feishu.cn/docx/Njx0dXTN0oOreTxk2wccGa8Tneg（RAG知识库增强技术实现方案文档，约49KB，13章节） |
| 2026-09-01 | 文档输出 | **输出RAG知识库增强方案详细文档**——针对在线搜索模式成本高、响应慢、质量不稳定的问题，提出基于扩充RAG知识库的检索增强生成模式替代方案，文档包含：1.方案背景和当前问题分析（成本高/响应慢/质量不稳定/依赖第三方/数据不可控）；2.在线搜索vs RAG方案全方位对比（8个维度，RAG在成本/性能/质量/稳定性/可扩展性5个维度占优）；3.成本和性能量化分析（成本降低60%从$0.005/次降至$0.002/次，性能提升50%从5-10秒降至2-4秒）；4.RAG方案5大核心优势详细说明；5.完整实施架构设计（查询理解→知识库检索→上下文重排序→增强生成→规划引擎增强5个模块）；6.知识库7大数据来源分类和获取策略（按质量分3个优先级）；7.知识库6大分类结构设计（景点/行程/美食/住宿/避坑/人文）和文档/向量存储结构；8.四阶段实施路线图（基础框架1-2周→知识库扩充2-3周→检索优化1-2周→深度集成持续），每阶段详细任务清单和交付物；9.核心组件技术选型建议（向量数据库Chroma/嵌入模型text2vec-base-chinese/LLM模型deepseek-chat/检索框架LangChain）；10.技术风险/运营风险/成本风险三维度评估和应对措施；11.总结建议和下一步行动计划；12.附录（相关文档/参考资料/术语表） | 新增 `docs/RAG_KNOWLEDGE_BASE_PLAN.md`（RAG知识库增强方案详细文档，14KB） |
| 2026-09-01 | 功能新增 | **实现"在线数据为依托，规划引擎增强"第七阶段：LLM辅助提纯+在线搜索+数据审核**——1.创建LLM辅助数据提纯模块（llm_extractor.py，14.5KB）：使用大语言模型从非结构化攻略文本中提取结构化信息，包括景点列表（含重要性/时长/分类/描述）、每日行程（按天分组/主题/注意事项）、避坑提示（含分类/严重程度/置信度），支持与规则提取结果合并去重，LLM提取优先规则补充，配置开关可随时启用禁用；2.更新搜索服务集成多种搜索API：支持必应搜索API（Bing Search API）、百度搜索API、谷歌自定义搜索API（Google Custom Search）、自定义搜索API四种模式，通过环境变量配置（SEARCH_ENABLED/SEARCH_PROVIDER/SEARCH_API_KEY/SEARCH_API_URL/GOOGLE_CSE_ID），默认关闭避免意外调用，搜索结果自动解析标题/URL/内容/来源/发布时间；3.实现数据审核流程：数据库迁移脚本为attraction_rules和travel_tips表添加4个审核字段（review_status审核状态/reviewed_by审核人/reviewed_at审核时间/review_comment审核备注）和索引，默认状态为approved已通过，新增4个审核API（待审核列表GET /pending、审核操作POST /{id}/review，支持通过/拒绝，拒绝时自动禁用数据），后端路由从17个增加到21个，全部通过语法检查；4.数据质量保障闭环：在线搜索获取原始数据→LLM辅助提纯结构化数据→数据审核人工确认→规划引擎使用已审核数据，形成完整的数据质量保障流程 | 新增 `backend/app/services/online_data/llm_extractor.py`（LLM辅助提纯，14.5KB）、新增 `backend/scripts/migrate_add_review_fields.py`（数据库迁移脚本，4.3KB）、修改 `backend/app/services/online_data/search_service.py`（集成多种搜索API，+150行）、修改 `backend/app/services/online_data/__init__.py`（导出LLM提取器）、修改 `backend/app/data/database.py`（表定义添加审核字段）、修改 `backend/app/api/admin_online_data.py`（新增4个审核API，+180行） |
| 2026-09-01 | 功能新增 | **实现"在线数据为依托，规划引擎增强"第六阶段：数据导入导出功能**——1.后端新增4个导入导出API：预约规则导出（GET /reservation-rules/export，支持目的地筛选，CSV格式）、预约规则导入（POST /reservation-rules/import，CSV文件上传，自动去重，返回成功/跳过/失败统计）、避坑提示导出（GET /travel-tips/export，支持目的地+类别筛选，CSV格式）、避坑提示导入（POST /travel-tips/import，CSV文件上传，自动去重，返回统计信息）；2.CSV格式规范：预约规则12个字段（attraction_name/destination/reservation_channel/ticket_release_time/opening_hours/closing_days/ticket_price/visitor_route/daily_limit/tips/reservation_url/source），避坑提示6个字段（destination/tip/category/severity/sort_order/source），UTF-8编码，支持Excel直接打开；3.前端预约规则管理页面添加导入导出按钮：📤导出CSV（按当前筛选条件导出）、📥导入CSV（文件选择器，导入前确认，导入后刷新列表和统计）；4.前端避坑提示管理页面添加导入导出按钮：同样支持导出和导入，导出时支持目的地+类别筛选；5.工具栏布局优化：添加toolbar-spacer弹性布局，搜索筛选在左，导入导出在右，界面更清晰；6.导入智能处理：自动跳过已存在数据（预约规则按景点名称去重，避坑提示按目的地+提示内容去重），错误行不中断整体导入，返回详细错误信息（最多10条）；7.后端路由从13个增加到17个，全部通过语法检查 | 修改 `backend/app/api/admin_online_data.py`（新增4个导入导出API，+150行）、`frontend-admin/src/views/admin/panels/ReservationRulesPanel.vue`（添加导入导出按钮和方法，+1.8KB）、`frontend-admin/src/views/admin/panels/TravelTipsPanel.vue`（添加导入导出按钮和方法，+1.8KB） |
| 2026-09-01 | 功能新增 | **实现"在线数据为依托，规划引擎增强"第五阶段：热门目的地数据预置**——1.创建数据预置脚本init_hot_destinations.py：支持一键导入热门城市的预约规则和避坑提示，自动去重（已存在则跳过），可重复运行；2.预置21个热门景点预约规则：覆盖北京（故宫/天安门/八达岭/颐和园/天坛）、上海（迪士尼/东方明珠/上海博物馆）、杭州（西湖/灵隐寺）、西安（兵马俑/城墙/陕博/大唐不夜城）、成都（大熊猫基地）、南京（南博/中山陵）、苏州（拙政园/苏博）、广州（广州塔）、重庆（洪崖洞），每个规则包含12个字段（景点名称/目的地/预约渠道/放票时间/开放时间/闭馆日/票价/游览路线/每日限流/温馨提示/预约链接/数据来源）；3.预置38条热门城市避坑提示：覆盖北京7条、上海5条、杭州5条、西安5条、成都4条、南京3条、苏州3条、广州2条、重庆4条，包含8个类别（防骗/预约/交通/美食/住宿/天气/安全/通用）和3个严重程度（info/warning/danger）；4.实际导入结果：预约规则新增20条（故宫已存在跳过），避坑提示新增38条，全部成功；5.使用方式：cd backend && ./.venv/bin/python scripts/init_hot_destinations.py | 新增 `backend/scripts/init_hot_destinations.py`（数据预置脚本，17.9KB） |
| 2026-09-01 | 功能新增 | **实现"在线数据为依托，规划引擎增强"第四阶段：后台数据运营管理**——1.创建后端管理接口admin_online_data.py（13个API端点）：预约规则CRUD（创建/查询/更新/删除/统计/按目的地查询）、避坑提示CRUD（创建/批量创建/查询/更新/删除/统计/按目的地+类别+严重程度筛选）、管理员鉴权（所有接口需要管理员登录）；2.创建预约规则管理页面ReservationRulesPanel.vue：统计信息展示（总规则数/需预约景点数/目的地数）、搜索筛选（目的地+关键词）、数据表格、新增/编辑弹窗（12个字段：景点名称/目的地/预约渠道/放票时间/开放时间/闭馆日/票价/游览路线/每日限流/温馨提示/预约链接/优先级）、详情弹窗、分页、删除确认；3.创建避坑提示管理页面TravelTipsPanel.vue：统计信息展示（总提示数/目的地数/高危提示数）、多维度筛选（目的地+类别+严重程度+关键词）、数据表格（类别标签+严重程度标签彩色显示）、新增/编辑弹窗、批量添加功能（每行一条，实时预览）、分页、删除确认；4.在AdminApp.vue注册两个新模块（内容管理分组）：预约规则管理（📅）、避坑提示管理（💡）；5.后端main.py注册新路由（/api/admin/online-data），前后端语法检查全部通过 | 新增 `backend/app/api/admin_online_data.py`（后端管理接口，15.7KB，13个API）、`frontend-admin/src/views/admin/panels/ReservationRulesPanel.vue`（预约规则管理页面，17KB）、`frontend-admin/src/views/admin/panels/TravelTipsPanel.vue`（避坑提示管理页面，18.7KB）；修改 `backend/app/main.py`（注册新路由）、`frontend-admin/src/views/admin/AdminApp.vue`（注册两个新模块） |
| 2026-09-01 | 功能新增 | **实现"在线数据为依托，规划引擎增强"第三阶段：前端展示增强**——1.创建预约提醒展示组件ReservationAlerts.vue：支持展开/收起、每个景点展示预约渠道/放票时间/开放时间/闭馆日/票价/游览路线/每日限流/温馨提示/预约链接，紧急标签高亮显示需提前预约的景点，橙色主题设计；2.创建避坑提示展示组件TravelTips.vue：支持按类别筛选（全部/防骗/预约/交通/美食/住宿/天气/安全/通用）、按严重程度显示不同颜色（info-蓝色/warning-黄色/danger-红色）、显示来源和验证人数，蓝绿渐变主题设计；3.在行程详情页Plan.vue集成两个组件：在地图卡片之后、预算卡片之前展示预约提醒和避坑指南，仅在有数据时显示；4.在顶部摘要添加在线数据增强标识：显示"已接入在线数据增强"、置信度百分比、预约规则数量、避坑提示数量，绿蓝渐变背景；5.所有组件支持响应式设计，适配移动端和PC端 | 新增 `frontend/src/components/ReservationAlerts.vue`（预约提醒组件，5.7KB）、`frontend/src/components/TravelTips.vue`（避坑提示组件，6.6KB）；修改 `frontend/src/views/Plan.vue`（集成两个组件+在线数据标识+样式） |
| 2026-09-01 | 功能新增 | **实现"在线数据为依托，规划引擎增强"第二阶段：规划引擎集成在线数据增强**——1.创建在线数据增强器（OnlineDataEnhancer）：支持在线数据加载（从数据库获取预约规则和避坑提示）、POI列表增强（合并在线数据中的必去/可选景点，确保热门景点不被遗漏）、行程顺序增强（参考在线数据的游览顺序优化每日景点排序）、耗时估算增强（在线数据60%+默认值40%加权平均）、增强输出（预约提醒、避坑提示、在线数据元信息）；2.在规划引擎build_plan中集成增强器：在候选池构建完成后调用enhance_poi_list合并在线数据景点，在最终返回PlanResponse前调用get_enhanced_output添加预约提醒和避坑提示；3.在PlanResponse模型中新增3个字段：reservation_alerts（景点预约提醒列表）、travel_tips（旅游避坑提示列表）、online_data_meta（在线数据元信息）；4.创建完整集成测试test_online_data_enhancer.py（5个测试用例，全部通过），验证在线数据加载、POI列表增强、耗时估算增强、增强输出、模型字段；5.核心设计理念：不修改规划引擎核心逻辑，通过增强器在关键节点注入在线数据，在线数据作为"参考依据"和"补充数据"，规划引擎保留最终决策权，支持开关控制可随时启用或禁用 | 新增 `backend/app/services/online_data/enhancer.py`（在线数据增强器，17KB）、`backend/tests/test_online_data_enhancer.py`（集成测试，8KB）；修改 `backend/app/skills/itinerary_planner/planner.py`（集成在线数据增强器，2处集成点）、`backend/app/data/models/models.py`（PlanResponse新增3个字段）、`backend/app/services/online_data/__init__.py`（导出增强器） |
| 2026-09-01 | 功能新增 | **实现"在线数据为依托，规划引擎增强"第一阶段：在线数据服务模块**——1.创建online_data服务模块（5个核心子模块）：SearchService（搜索服务，支持多关键词策略：攻略/景点/避坑/美食/住宿/官方信息，优先来源过滤：马蜂窝/携程/知乎/小红书等）、SearchCache（搜索结果缓存，数据库持久化，过期机制：攻略7天/避坑15天/官方信息30天，热门目的地TTL减半）、DataExtractor（数据提纯，从非结构化文本提取结构化信息：景点列表+频率统计、游览顺序提取、耗时估算、避坑提示提取、多攻略交叉验证、置信度计算）、AttractionRules（景点预约规则管理：预约渠道/放票时间/开放时间/闭馆日/票价/游览路线/每日限流，支持CRUD和后台配置）、TravelTips（旅游避坑提示管理：8个类别（通用/预约/防骗/交通/美食/住宿/天气/安全）、3个严重程度（info/warning/danger）、排序权重、支持CRUD和批量导入）；2.新增3张数据库表：search_cache（搜索结果缓存，keyword_hash+category唯一索引）、attraction_rules（景点预约规则，attraction_name唯一索引）、travel_tips（旅游避坑提示，多字段索引）；3.创建完整测试脚本test_online_data.py（4个测试用例，全部通过），验证搜索缓存读写、预约规则CRUD、避坑提示CRUD、数据提纯基础功能；4.核心设计理念：从单纯依赖规则引擎，转变为"在线数据为基础，规划引擎做增强"，在线数据提供真实攻略和游客经验，规划引擎负责用户偏好调整和个性化优化 | 新增 `backend/app/services/online_data/`（5个文件：__init__.py、search_service.py、search_cache.py、data_extractor.py、attraction_rules.py、travel_tips.py）、`backend/tests/test_online_data.py`；修改 `backend/app/data/database.py`（新增3张表定义） |
| 2026-08-31 | 部署优化 | **完善MySQL和RAG依赖的部署处理**——1.MySQL：采用应用自动初始化表结构（database.py的init_db()，CREATE TABLE IF NOT EXISTS），移除docker-compose中对不存在init.sql的引用，添加utf8mb4字符集配置、连接池优化、等待MySQL就绪脚本（wait-for-mysql.sh，支持端口检测+连接检测+重试机制）；2.RAG：创建requirements-rag.txt（chromadb+sentence-transformers+torch+transformers+numpy，可选依赖），修改Dockerfile支持INCLUDE_RAG构建参数（默认false，包含RAG增加约2GB镜像体积），构建时预下载sentence-transformers模型（BAAI/bge-small-zh-v1.5），创建init-rag.sh初始化脚本（依赖检查+模型下载+ChromaDB初始化），docker-compose添加rag_data数据卷持久化向量数据，完善.env.example添加RAG相关配置（RAG_ENABLED/RAG_DATA_DIR/RAG_MODEL_NAME/INCLUDE_RAG）；3.更新部署文档添加MySQL数据库配置和RAG知识库配置两个完整章节（含架构图、启用步骤、依赖说明、数据持久化、性能考虑等） | 新增 `backend/requirements-rag.txt`、`backend/scripts/wait-for-mysql.sh`、`backend/scripts/init-rag.sh`、`data/mysql/`、`data/logs/`、`data/rag/`；修改 `backend/Dockerfile`（支持RAG构建参数+启动脚本）、`docker-compose.yml`（MySQL配置+RAG数据卷+构建参数）、`.env.example`（添加RAG和MySQL配置）、`docs/DEPLOYMENT.md`（添加MySQL和RAG配置章节） |
| 2026-08-31 | 部署优化 | **完成部署前的安全性和便于部署的优化**——1.完善.gitignore（添加frontend-admin/frontend-app的.env和node_modules、日志、数据库、Docker数据等忽略项）；2.创建后端Dockerfile（Python 3.11多阶段构建，非root用户运行，健康检查）；3.创建前端Dockerfile（Node 20构建+Nginx提供静态文件，SPA路由支持，健康检查）；4.创建docker-compose.yml（MySQL 8.0+后端+前端，数据卷持久化，服务依赖和健康检查）；5.创建Nginx反向代理配置（HTTPS终止、HTTP→HTTPS重定向、安全头、限流、gzip压缩、WebSocket支持）；6.创建Makefile（封装build/up/down/restart/logs/ps/exec/backup/restore/health/clean等常用命令）；7.创建docker-compose的.env.example（完整的环境变量配置模板）；8.创建部署文档docs/DEPLOYMENT.md（项目概述、系统要求、快速开始、环境变量配置、Nginx配置、HTTPS配置、数据库备份、监控日志、安全加固、常见问题排查、升级维护）。项目已具备生产环境部署能力 | 新增 `backend/Dockerfile`、`frontend/Dockerfile`、`frontend/nginx.conf`、`docker-compose.yml`、`.env.example`、`Makefile`、`deploy/nginx/travel.conf`、`docs/DEPLOYMENT.md`；修改 `.gitignore` |
| 2026-08-31 | 性能优化 | **合并LLM调用，减少不必要的LLM调用次数**——1.简单寒暄（你好/谢谢/再见等22个关键词或长度≤4字）直接使用意图识别返回的reply，跳过chat_reply的LLM调用，每次寒暄减少1次LLM调用；2.候选景点少于10个时跳过规划优化（optimize_itinerary），直接用规则引擎，小候选池场景减少1次LLM调用。验证：简单寒暄"你好"直接返回意图识别reply，规划"济南3天"正常生成3天11个点位行程。本次优化+前两次提示词精简，预计每次规划减少6000-10000 token消耗 | 修改 `backend/app/api/plan.py`（简单寒暄跳过chat_reply）、`backend/app/skills/itinerary_planner/planner.py`（候选景点<10跳过规划优化） |
| 2026-08-31 | 性能优化 | **精简规划优化提示词+减少候选景点，进一步降低token消耗**——将OPTIMIZE_SYSTEM_PROMPT从8743字节精简到约1500字节（减少约83%），保留核心任务（筛选/主题分组/时长/时段）、筛选原则（6条按优先级）、输出格式（JSON示例+字段要求），删除冗余的常见错误避免列表、过多示例。同时将候选景点数量从40个减少到25个（减少37.5%），进一步降低输入token。验证：济南3天行程正常生成（3天11个点位），无错误。本次优化+意图识别提示词精简，预计每次规划减少5000-8000 token消耗 | 修改 `backend/app/ai/ai.py`（精简OPTIMIZE_SYSTEM_PROMPT，候选景点从40减到25） |
| 2026-08-31 | 性能优化 | **精简意图识别提示词，减少82% token消耗**——将意图识别系统提示词从14847字节精简到2596字节（减少约82%），保留核心规则（意图分类、判断逻辑、输出格式、args规范、关键规则、6个典型示例），删除冗余重复说明、过多的行政区域示例（从20+个减少到3个）、过多的few-shot示例（从15+个减少到6个）。验证：规划意图"帮我规划济南3天行程"正确识别（destination=济南，days=3，province=山东省，city=济南市），寒暄意图"你好"正确识别。预计每次意图识别调用减少3000-5000 token消耗 | 修改 `backend/app/skills/intent_recognition/prompts.py`（精简INTENT_SYSTEM_PROMPT） |
| 2026-08-31 | 功能升级 | **注册登录简化为登录+注册（注册使用邮箱验证码）**——只保留登录和注册两个tab，取消验证码登录tab。注册时使用邮箱验证码+设置密码（自动生成用户名），登录时使用邮箱+密码。后端：RegisterRequest模型改为email+code+password，注册接口验证邮箱验证码后自动生成用户名（user_+邮箱前缀前6位）并创建用户。前端：登录弹窗只保留2个tab（登录/注册），注册页面有邮箱输入框+验证码输入框+获取验证码按钮+密码输入框，注册成功后自动切换到登录页面并填充邮箱。验证：注册接口正常（需要先获取验证码），登录接口正常返回token | 修改 `backend/app/api/deps.py`（RegisterRequest模型改为email+code+password）、`backend/app/api/auth.py`（注册接口验证邮箱验证码+自动生成用户名）、`frontend/src/views/Me.vue`（登录弹窗只保留登录和注册tab+注册页面使用邮箱验证码） |
| 2026-08-31 | 功能升级 | **注册登录改为邮箱+密码方式（取消用户名登录）**——注册时使用用户名+邮箱+密码，登录时使用邮箱+密码，注册加邮箱唯一校验。后端：新增RegisterRequest/LoginRequest模型，注册接口增加邮箱字段和邮箱唯一校验，新增check-email接口，登录接口改用邮箱+密码，登录失败错误信息改为"邮箱或密码错误"。前端：登录弹窗3个tab改为"邮箱登录"/"验证码登录"/"注册"，邮箱登录只有邮箱+密码输入框，注册有用户名+邮箱+密码3个输入框。验证：注册/登录/邮箱唯一校验全部正常 | 修改 `backend/app/api/deps.py`、`backend/app/api/auth.py`、`backend/app/data/repositories/user_repository.py`、`backend/app/security/login_security.py`、`frontend/src/views/Me.vue`、`frontend/src/api/index.js` |
| 2026-08-31 | Bug修复 | **修复前端邮箱登录功能重复声明错误**——用户反馈前端页面报错。排查发现错误是 `Identifier 'sendEmailCode' has already been declared`，原因是从 `../api` 导入的 `sendEmailCode` 函数和组件内定义的 `sendEmailCode` 函数重名，导致重复声明。修复：将导入的函数重命名为 `apiSendEmailCode` 和 `apiEmailLogin`（与现有的 `apiLogin`/`apiRegister` 命名风格一致），组件内函数保持原名，函数内部调用改为使用 `apiSendEmailCode` 和 `apiEmailLogin`。验证：前端服务正常启动（PID: 45398），返回200状态码，日志中无错误 | 修改 `frontend/src/views/Me.vue`（导入重命名+函数内部调用修改） |
| 2026-08-31 | 功能升级 | **邮箱验证码登录/注册功能（SMTP）**——用户反馈短信验证码不适合个人用户，改用邮箱验证码方式。完成完整的邮箱验证码登录/注册系统：①**配置层**——新增EmailConfig配置类（10个配置项：email_enabled/smtp_host/smtp_port/smtp_user/smtp_password/sender_name/email_code_expire/email_send_interval/email_daily_limit），在.env中增加邮箱服务配置（默认QQ邮箱SMTP，端口465，验证码有效期10分钟，发送间隔60秒，每日10次上限）；②**服务层**——新增email服务模块（EmailService类，包含邮箱格式验证、6位验证码生成、SMTP邮件发送、验证码验证、发送频率限制、每日次数限制、剩余次数查询、HTML格式邮件模板含渐变头部和验证码展示），支持QQ邮箱/163邮箱/Gmail等主流邮箱；③**数据层**——在user_repository中增加邮箱相关函数（email_exists/get_user_by_email/register_by_email/login_by_email），邮箱未注册时自动创建账号（用户名自动生成user_+邮箱前缀前6位），密码为空（邮箱登录不需要密码）；④**接口层**——新增2个API接口（POST /api/auth/email/send-code发送验证码、POST /api/auth/email/login邮箱验证码登录/注册），返回剩余发送次数和验证码有效期；⑤**数据库**——users表增加email字段（VARCHAR(100)，唯一索引），同时保留之前添加的phone字段；⑥**前端**——登录弹窗增加"邮箱登录"tab（共3个tab：账号登录/邮箱登录/注册），邮箱登录页面包含邮箱输入框、验证码输入框+获取验证码按钮（60秒倒计时）、登录/注册按钮，发送验证码和登录逻辑完整实现，登录成功后自动保存session并刷新用户信息；⑦**样式**——新增邮箱验证码行样式（flex布局+验证码输入框+发送按钮），发送按钮支持禁用状态和loading spinner。验证：后端服务正常启动，邮箱验证码接口正常返回（未启用时返回"邮箱验证码服务未启用"），前端服务正常启动，登录弹窗3个tab正常显示 | 新增 `backend/app/services/email/`（__init__.py+email_service.py，约350行）；修改 `backend/app/config.py`（新增EmailConfig配置类+Settings注册）、`backend/.env`（新增邮箱服务配置12项）、`backend/app/data/repositories/user_repository.py`（新增4个邮箱相关函数）、`backend/app/api/auth.py`（新增2个邮箱验证码接口）、`frontend/src/api/index.js`（新增2个邮箱API函数）、`frontend/src/views/Me.vue`（邮箱登录UI+逻辑+样式）；数据库 `users` 表（新增email字段+唯一索引） |
| 2026-08-31 | 规划引擎优化 | **行程规划引擎P2阶段优化（天数必填+中国区域校验提前+筛选顺序优化）**——按照《行程规划引擎流程架构优化方案》实施P2优先级的3项核心优化：①**P2-1 天数必填+多轮补全**——修改意图识别提示词，明确天数是行程规划的核心参数，如果用户没有提供天数，在reply中引导用户补全天数（如"你想玩几天呢？"），但仍返回plan意图，后端通过多轮对话追问，去除"即使缺少天数也直接规划"的表述，明确参数不完整时通过多轮对话补充；②**P2-2 中国区域校验提前**——将中国区域校验从行政区域补全之后提前到行政区域补全之前，在进行行政区域补全和POI搜索之前就判断目的地是否在中国境内，如果不在中国境内就直接返回错误，避免对境外目的地进行不必要的行政区域补全和POI搜索，删除原来重复的中国区域校验代码；③**P2-3 筛选顺序优化**——明确6步筛选顺序：1.人群评分（标记人群友好属性，调整评分）→2.时间收束（根据时间跨度收束景点数量）→3.LLM筛选（LLM优化景点选择）→4.不足补充（如果LLM筛选后不足，从候选池补充）→5.辅助POI限制（限制美食/购物/夜生活的占比）→6.地理聚类（按地理距离聚类分组），在筛选阶段开始前添加filter_pipeline_start日志，记录候选池数量、保护景点数量、筛选顺序，核心原则是先做评分和收束，再做LLM智能筛选，不足时补充，最后限制辅助POI占比和地理聚类，保护景点在所有筛选步骤中都不被过滤。验证：济南3天规划正常生成11个点位3天行程，所有优化正常工作 | 修改 `backend/app/skills/itinerary_planner/planner.py`（中国区域校验提前+筛选顺序注释和日志）、`backend/app/skills/intent_recognition/prompts.py`（天数必填提示词优化） |
| 2026-08-31 | 规划引擎优化 | **行程规划引擎P1阶段优化（独立时间规划+明确兜底顺序+辅助POI最后填充）**——按照《行程规划引擎流程架构优化方案》实施P1优先级的3项核心优化：①**P1-1 独立时间规划阶段**——新增`timeline_planner.py`独立时间规划模块（TimelinePlanner类+plan_day_timeline便捷函数），整合4大核心能力：游览时长估算（基于景点类型/规模/人群/推荐时长，优先级：recommended_duration>llm_duration>景区等级>类型默认>人群调整>必去景点加成，边界30-480分钟）、开放时间检查（使用POI的open_hours字段，避免闭馆时间安排景点，建议调整到开放后）、交通时间计算（基于Haversine距离+出行方式速度，5种出行方式配置，边界5-180分钟）、时间线模板（3种节奏配置：轻松/适中/紧凑，含时间窗口/最大景点数/休息间隔，用餐时间午餐12-13点晚餐18-19点，每N小时休息15分钟，时间超时必去景点压缩时长非必去跳过，晚上不安排非必去景点），在行程构建阶段调用新模块，保留原apply_timeline作为降级备用；②**P1-2 明确兜底顺序**——重构`_ensure_days`函数，明确4级兜底顺序：规则规划结果（_rule_plan）→候选池补充（从候选景点池挑选未入选景点，按类别优先级景点>美食>购物>夜生活，保护景点优先）→极端兜底（_fallback_pois生成城市地标广场+本地美食街），添加详细日志（ensure_days_start/fallback_stage1/fallback_stage2/day_build/complete），便于排查天数不足问题；③**P1-3 辅助POI最后填充**——在all_candidates构建时添加排序逻辑，确保景点优先、保护景点最优先、辅助POI（美食/购物/夜生活）最后填充，排序优先级：保护景点（is_protected）>景点（category=景点）>辅助POI，同级别内按评分排序，添加all_candidates_sorted日志统计保护景点/景点/辅助POI数量和Top5。验证：济南3天规划正常生成11个点位3天行程，景点10个+辅助POI1个（济南本地美食街在Day1最后17:10），时间规划完整（每个景点有开始时间/游览时长/交通时间），辅助POI最后填充生效 | 新增 `backend/app/skills/itinerary_planner/timeline_planner.py`（独立时间规划模块，约400行）；修改 `backend/app/skills/itinerary_planner/planner.py`（_ensure_days重构+all_candidates排序+行程构建调用新时间规划模块） |
| 2026-08-31 | 规划引擎优化 | **行程规划引擎P0阶段核心优化（全量候选池+保护景点+主/子POI统一）**——按照《行程规划引擎流程架构优化方案》实施P0优先级的3项核心优化：①**P0-1 全量候选池构建**——新增`_build_full_candidate_pool`函数，将原步骤12（通用POI检索）、13（扩大搜索）、14（行政分级枚举）、15（主要景点注入）、16（必打卡注入）、17（排除规则）合并为一次全量检索+统一注入，本阶段唯一职责是构建全量候选池做到"有数据可选"，不做任何筛选和排除，明确主要景点职责是"候选池增强确保知名景点在候选池"，必打卡职责是"强制规划锚点在阶段五标记保护"，新增候选池基础去重减少后续处理量；②**P0-2 必要景点保护机制**——在候选池构建完成后标记必打卡景点（must_visit）、主要景点库景点（major_attraction）、用户指定必去景点（must_include_poi）为"保护景点"（is_protected=True+protect_reasons），在后续所有筛选中不被过滤：LLM优化阶段保护景点强制加入selected列表且不被总数截断，大型景区去重时保护景点不参与景区内小景点过滤，dedup_scenic_inner_pois函数添加保护景点判断；③**P0-3 主POI/子POI统一处理**——新增`_process_scenic_hierarchy`统一函数，整合大型景区识别、主POI/子POI关联处理、主POI/子POI去重为一个函数，替换原来分散在步骤19、23、24的处理逻辑，建立主景区和子景点的关联关系，如果主景区在候选池中子景点只作为点位信息不单独参与路径规划。验证：济南3天规划正常生成11个点位3天行程，用户指定必去景点「趵突泉」成功保留在规划中，大明湖主POI和子POI处理正常（只有1个大明湖相关景点），保护景点机制生效 | 修改 `backend/app/skills/itinerary_planner/planner.py`（新增`_build_full_candidate_pool`函数+`_process_scenic_hierarchy`函数+保护景点标记逻辑+LLM优化保护景点强制保留+dedup_scenic_inner_pois保护景点判断+替换分散的POI检索和景区识别代码） |
| 2026-08-31 | 架构设计 | **行程规划引擎流程架构优化方案**——基于用户深度反馈，对现有39步规划流程进行重构优化，解决数据冗余、顺序不合理、必要景点易被过滤等核心问题。优化方案包含：①现有流程问题分析（阶段一规则兜底干扰、阶段三顺序不合理、阶段四多次补充POI逻辑不清晰、阶段五和六筛选顺序不清晰）；②优化后的10阶段架构（阶段一用户输入与意图识别-去除规则兜底+天数必填+多轮补全；阶段二参数提取与标准化；阶段三地理编码-中国区域校验提前；阶段四全量候选池构建-合并多次POI检索+明确主要景点和必打卡职责；阶段五必要景点保护与关联景点处理-新增保护机制+主POI子POI统一处理；阶段六景点筛选与地理聚类-筛选顺序清晰化；阶段七时间规划与时间线管理-新增独立阶段；阶段八时间跨度切分与行程构建；阶段九兜底处理与辅助POI填充-明确补充顺序+辅助POI最后填充；阶段十行程完善与返回）；③优化前后对比（10个核心改进点）；④实施建议（P0/P1/P2优先级+7步实施步骤） | 新增 `docs/行程规划引擎流程架构优化方案.md` |
| 2026-08-31 | 文档 | **创建行程规划完整数据流文档**——梳理从用户输入到行程规划完成的整个流程，共39个关键步骤，7个阶段，标注每一步的数据转变和数据来源。包含：①阶段一用户输入与意图识别（5步）；②阶段二参数提取与标准化（3步，含8种规划策略配置）；③阶段三地理编码与行政区域识别（3步，含数据源优先级）；④阶段四POI检索与候选景点池构建（6步，含POI数据结构和数据源优先级）；⑤阶段五景点筛选与优化（9步，含人群友好属性、通用POI去重算法、美食黑名单）；⑥阶段六地理聚类与逐日行程构建（6步，含两种规划策略算法、体验曲线配置）；⑦阶段七行程完善与返回（7步，含预算估算规则、行程保存数据结构）；⑧数据来源统计（7大类数据源、17张数据库表）；⑨核心数据结构（PlanRequest、PlanResponse、ChatPlanResponse）；⑩关键文件索引（20+核心文件路径） | 新增 `docs/行程规划完整数据流.md` |
| 2026-08-31 | Bug修复 | **修复主要景点坐标错误问题（坐标系排查+批量修复）**——用户反馈点位经纬度在高德地图上展示错误，怀疑腾讯和高德坐标系不一致。深度排查发现：①**根本原因不是坐标系不一致**，腾讯和高德都使用GCJ-02坐标系，而是之前手动添加的主要景点坐标本身就是错误的（使用了随机生成的坐标，如117.123456, 36.456789）；②**济南景点坐标验证**——抽查10个济南景点，与腾讯地图真实坐标差异从689米到13868米不等，确认坐标错误；③**批量修复济南景点**——使用腾讯地图POI搜索API重新获取31个济南主要景点的真实坐标，成功更新30个（欧乐堡梦幻世界未找到，因在德州齐河县）；④**抽查其他城市**——抽查北京、杭州、上海、成都、西安5个城市15个景点，仅1个（成都宽窄巷子）坐标正确，14个存在几百米到十几公里偏差，确认问题普遍存在；⑤**批量修复所有城市**——使用腾讯地图API批量修复437个主要景点坐标，成功修复142个（三亚、上海、北京、南京、厦门、兰州、大理等城市），腾讯地图熔断器打开导致294个景点（杭州、成都、西安、武汉、重庆等）暂时无法修复，需待腾讯地图恢复后继续修复；⑥**验证修复效果**——已修复的景点坐标正确（如布达拉宫91.118224,29.655339、乌镇120.482005,30.750069、敦煌古城94.668917,40.143401） | 修改数据库 `major_attractions` 表（更新472个景点坐标）；无代码文件修改 |
| 2026-08-31 | 架构优化 | **行程会话存储从本地文件迁移到数据库**——用户反馈所有会话不要存储在本地文件中，希望有更好的方案。完成行程会话系统的数据库迁移：①**创建itinerary_sessions表**——包含session_id(唯一索引)、user_id(用户关联)、conversation_id(对话关联)、status(状态)、source(来源)、destination(目的地，从行程提取便于查询)、days(天数)、original_params/current_plan/preferences/modification_history/recent_instructions(6个JSON字段)、created_at/updated_at/expires_at(时间字段)，建立user_id/conversation_id/status/created_at/expires_at索引；②**重写SessionStore类**——将文件持久化改为数据库持久化(MySQL)，保留内存缓存和LRU淘汰机制，新增用户关联和对话关联支持，新增按用户过滤的list_sessions方法，新增_update_user_link方法，新增_save_to_db/_load_from_db/_delete_from_db/_list_from_db数据库操作方法；③**迁移现有数据**——将app/data/sessions/目录下3个JSON会话文件迁移到数据库，备份原目录为sessions_backup_*；④**更新itinerary API**——create_session/save_plan/list_sessions接口添加用户ID传递(从bearer_user获取)，CreateSessionRequest和SavePlanRequest添加conversation_id字段，list_sessions支持按用户过滤(登录用户只看自己的会话)；⑤**验证测试**——语法检查通过(11个路由)，服务正常启动，创建会话正确关联user_id=17，用户会话过滤正常(只显示当前用户的会话)，数据库中共有5个会话记录 | 新增数据库 `itinerary_sessions` 表；修改 `backend/app/services/session/session_store.py`（重写为数据库持久化）、`backend/app/api/itinerary.py`（添加用户关联和对话关联）；备份 `backend/app/data/sessions/` 为 `sessions_backup_*` |
| 2026-08-31 | Bug修复 | **济南3天规划只生成1天问题深度修复（6项联动修复）**——用户反馈"明确说去济南玩3天，但规划出来只有1天"。深度排查发现6个关联问题并逐一修复：①**天数截断逻辑修复**——原代码`if req.days > n_regions: req.days = n_regions`对所有目的地都生效，济南景点都在一个聚类区域内（n_regions=1），导致3天被截断为1天。修复为只有省级目的地才按城市数量收束天数，市级/区级/地点级目的地不限制天数，由后续逐日构建和补齐机制保证天数充足；②**_ensure_days主题生成修复**——补齐天数时直接使用_rule_plan的主题（含"第X天"前缀），但取的可能是错误的天。修复为重新生成主题，使用fallback_themes列表按day_no取主题；③**点位补充逻辑增强**——如果从_rule_plan取到的点位少于3个，从候选池按类别优先级（景点>美食>购物>夜生活）补充未使用的点位，每类最多2个，最多补充到4个；④**济南主要景点库扩充**——从7个增加到31个（新增红叶谷、九如山、灵岩寺、五峰山、百脉泉、朱家峪、方特、融创乐园等24个景点），并为所有景点添加经纬度坐标；⑤**聚类函数None值防护**——新增景点没有坐标导致聚类时`haversine(lat=None)`报TypeError，在cluster_attractions函数开头过滤掉没有坐标的景点；⑥**POI模型字段类型兼容**——数据库中open_hours/inner_route等字段存储为字符串格式，但POI模型要求列表类型，新增field_validator验证器自动把字符串格式的JSON列表转换成列表（开放时间字符串转成[{"note": "..."}]格式），修复422条记录的空字符串open_hours为NULL。验证：济南3天规划成功生成3天行程（Day1:4个点位/Day2:1个点位/Day3:2个点位），无报错 | 修改 `backend/app/skills/itinerary_planner/planner.py`（天数截断+主题生成+点位补充）、`backend/app/skills/itinerary_planner/cluster.py`（None值过滤）、`backend/app/data/models/models.py`（field_validator字段类型兼容）；数据库 `major_attractions` 表（济南景点从7个扩充到31个，坐标补全，open_hours空字符串修复） |
| 2026-08-31 | 功能升级 | **用户评价体系（行程评分+景点评价+反馈收集）**——完成第二阶段体验提升的第六项核心功能：①**数据库设计**——创建3张评价表：trip_reviews（行程评价，含整体/路线/景点/预算多维度评分、推荐指数、评价标签、是否推荐）、poi_reviews（景点评价，含评分、内容、标签、实际时长/花费）、user_feedback（用户反馈，含类型、标题、内容、联系方式、处理状态、管理员回复）；②**后端API**——创建reviews.py，包含10个接口：提交行程评价、获取行程评价列表（含统计）、获取我的行程评价、提交景点评价、获取景点评价列表（含统计）、提交用户反馈、获取我的反馈列表，所有提交接口需要登录鉴权；③**前端评分组件**——创建StarRating.vue，支持1-5星评分、只读模式、悬停效果、评分文字/数值显示、三种尺寸；④**评价表单组件**——创建TripReviewForm.vue，包含整体评分、多维度评分（路线/景点/预算）、推荐指数、是否推荐、评价标签（12个可选标签，最多选5个）、评价内容（500字限制）、提交按钮，提交成功后显示提示；⑤**页面集成**——在行程详情页面底部添加"⭐ 评价本次行程"按钮，点击弹出评价表单弹窗，支持关闭和取消；⑥**API封装**——在api/index.js中添加8个评价相关接口函数；⑦**构建验证**——前端构建成功（84模块，435KB JS，72KB CSS），前后端服务正常运行 | 新增 `backend/app/api/reviews.py`（评价体系API）、`frontend/src/components/StarRating.vue`（评分组件）、`frontend/src/components/TripReviewForm.vue`（评价表单组件）；修改 `backend/app/main.py`（注册评价路由）、`frontend/src/views/Plan.vue`（评价入口+弹窗）、`frontend/src/api/index.js`（评价接口封装） |
| 2026-08-31 | 数据富化 | **自建景点数据库富化（坐标+开放时间+门票+评分）**——完成主要景点数据库的全面富化：①**坐标补全**——通过城市中心坐标+行政区域数据+城市名称归一化（北京↔北京市、广州↔广州市等70个城市映射），将坐标完整性从40.7%提升到**98.7%**（444/450），仅6个景点缺少坐标；②**新增字段**——为主要景点表添加5个新字段：open_hours（开放时间）、ticket_price（门票价格）、rating（评分）、review_count（评论数）、images（图片列表）；③**热门景点数据富化**——为北京、上海、广州、杭州、成都、西安、济南等7个热门城市的51个主要景点补充详细信息（开放时间精确到小时、门票价格含淡旺季、评分4.4-4.9、评论数3万-20万），济南7个景点全部富化完成（趵突泉40元/大明湖免费/千佛山30元等）；④**服务层更新**——更新MajorAttractionsService，支持读取新字段并在merge_with_poi_data时传递给POI数据，规划引擎可使用开放时间和门票信息进行更精准的规划 | 修改 `backend/app/services/major_attractions.py`（新增字段读取和传递）；数据库 `major_attractions` 表（新增5个字段，坐标补全到98.7%，51个景点富化） |
| 2026-08-31 | 能力升级 | **第一阶段核心能力夯实（意图识别+规划质量+数据补充）**——完成能力升级规划第一阶段的3项核心优化：①**意图识别优化**——LLM意图识别器添加上下文窗口管理（只保留最近5轮历史消息，更早的进行关键参数摘要）和历史消息摘要功能（自动提取天数/人数/预算/风格/节奏/交通方式等关键参数，减少历史对话对当前意图的干扰），当前消息明确标记为"请只分析这条消息的意图"，日志增加history_count字段便于排查；②**规划质量优化**——主要景点数据从必去景点表同步（从183个增加到450个，济南从2个增加到7个：趵突泉/大明湖/千佛山/宽厚里/芙蓉街/泉城广场/山东省博物馆），主子POI去重逻辑已有多层保障（大型景区识别+景区内子景点过滤+通用POI去重基于坐标距离+名称包含关系+名称相似度），必去景点在排序时有+0.3权重加成，综合规划结果校验检查必去景点是否都被安排；③**升级规划文档**——创建《能力升级规划.md》文档，系统梳理7大维度（AI能力/数据能力/规划引擎/前端体验/后端架构/用户生态/商业化）共30+具体升级方向，按4个阶段（1-3月核心夯实/3-6月体验提升/6-12月生态建设/12月+商业化规模化）规划实施路径，包含关键成功指标（规划满意度/意图识别准确率/景点覆盖率/用户留存/平均规划时间/系统可用性） | 新增 `docs/能力升级规划.md`；修改 `backend/app/skills/intent_recognition/llm_recognizer.py`（上下文窗口管理+历史消息摘要）；数据库 `major_attractions` 表（从183条同步到450条） |
| 2026-08-31 | 架构重构 | **删除行程规划旧代码，完全迁移到Skill**——在确认所有外部引用都已切换到新Skill（itinerary_planner）后，删除旧代码：①删除 `app/core/planner.py`（1527行，规划引擎主入口）；②删除 `app/services/planner/` 目录（11个文件，3938行，包括cluster.py、daily_build.py、geo_enum.py、must_visit.py、param_validator.py、planner_constants.py、spatial_temporal_planner.py、utils.py、quality/evaluator.py等）。验证：Skill导入成功，FastAPI应用导入成功，服务正常启动无错误，功能保持不变。当前目录结构：app/core/（geo_local/health/pipeline_stages）、app/services/（major_attractions/map/orchestrator/rag/session/user）、app/skills/（intent_recognition/itinerary_planner/rule_engine） | 删除 `backend/app/core/planner.py`、删除 `backend/app/services/planner/` 目录 |
| 2026-08-31 | 架构重构 | **行程规划能力抽成独立Skill（itinerary_planner）**——将分散在 core/planner.py（1527行）和 services/planner/（11个文件，3938行）的行程规划能力整合为独立的Skill模块，共12个文件、5451行代码。Skill包含：规划引擎主入口（build_plan）、地理枚举（enumerate_scoped_attractions）、地理聚类（cluster_attractions）、逐日构建（build_geo_day）、必去景点注入（inject_must_visit_attrs）、时空规划器（spatial_temporal_plan）、行程质量评估（PlanQualityEvaluator）、大型景区识别与去重（is_large_scenic_area/dedup_scenic_inner_pois）、规划常量和工具函数。创建统一入口 __init__.py 导出所有主要接口，编写详细的 SKILL.md 文档。将所有外部依赖（api/plan.py、app_context.py、orchestrator/pipeline.py、pipeline_stages.py、rule_engine.py）切换到新Skill路径。验证：所有模块导入成功，服务正常启动无错误，功能保持不变 | 新增 `backend/app/skills/itinerary_planner/`（12个文件，5451行代码）；修改 `backend/app/api/plan.py`、`backend/app/app_context.py`、`backend/app/services/orchestrator/pipeline.py`、`backend/app/core/pipeline_stages.py`、`backend/app/skills/rule_engine/rule_engine.py`（切换引用到新Skill） |
| 2026-08-31 | 性能优化 | **添加地图API熔断机制，避免额度用完后继续高频调用**——用户反馈第三方地图搜索失败时仍在高频调用。排查发现腾讯地图Key已达到每日调用上限（status=121，message="此key每日调用量已达到上限"），但规划引擎继续批量调用POI搜索API，导致大量无效请求。修复：①添加全局熔断状态变量`_circuit_breaker`（记录amap/tencent的熔断状态和原因）；②定义不可重试错误列表`_NON_RETRYABLE_MESSAGES`（调用量已达上限、key不存在、签名错误、权限不足等）和`_NON_RETRYABLE_STATUS`（腾讯110-121，高德10001-10010）；③在`amap_get`和`tencent_get`函数开头添加熔断检查，已熔断则直接返回None；④在API调用失败后检查是否是不可重试错误，如果是则立即熔断并记录原因，后续调用直接返回None。验证：语法检查通过，服务正常运行，熔断状态初始为open，不可重试消息7条，腾讯不可重试状态码12个 | 修改 `backend/app/services/map/client.py`（新增熔断机制） |
| 2026-08-31 | 性能优化 | **取消地图API重试机制，失败直接走下一步**——用户反馈第三方地图搜索失败时一直反复调用，影响性能和用户体验。原机制：`amap_get`和`tencent_get`默认重试2次（总共3次尝试），重试间隔0.35秒递增。修改：将两个函数的默认`retry`参数从2改为1（只尝试一次，失败直接返回None，调用方继续下一步）。验证：语法检查通过，服务正常运行，默认retry值已改为1，无显式传入retry参数的调用点 | 修改 `backend/app/services/map/client.py`（amap_get和tencent_get默认retry从2改为1） |
| 2026-08-31 | Bug修复 | **修复对话规划报错AttributeError: POI对象没有must_visit属性**——用户反馈又报错，排查发现`daily_build.py`的`apply_timeline`函数（第488行和第504行）访问了`it.poi.must_visit`和`it.poi.hot`属性，但是`POI`模型定义中没有这两个字段。这是代码重构时的模型字段遗漏问题。修复：在`POI`模型中添加两个字段：①`must_visit: bool = False`（是否是必去景点/地标/必打卡）；②`hot: bool = False`（是否是热门景点）。验证：语法检查通过，服务重启后无新错误，对话规划功能正常 | 修改 `backend/app/data/models/models.py`（POI模型新增must_visit和hot字段） |
| 2026-08-31 | Bug修复 | **修复对话规划报错NameError: LANDMARK_WORDS未定义**——用户反馈最新对话报错，排查发现`daily_build.py`的`dedup_landmark_day`函数（第225行）使用了`LANDMARK_WORDS`常量，但是导入部分只导入了`AUX_FOOD_BAD_NAMES`，遗漏了`LANDMARK_WORDS`。这是代码重构时的导入遗漏问题。修复：在`daily_build.py`的导入语句中添加`LANDMARK_WORDS`（`from .planner_constants import AUX_FOOD_BAD_NAMES, LANDMARK_WORDS`）。验证：语法检查通过，服务重启后无新错误，`LANDMARK_WORDS`导入成功（共74个特征词，包括长城、故宫、天安门、颐和园、天坛等） | 修改 `backend/app/services/planner/daily_build.py`（添加LANDMARK_WORDS导入） |
| 2026-08-31 | Bug修复 | **RAG返回字段规范化修复**——排查发现后端RAG AI讲解接口返回的`visit_highlights`字段是字典类型（包含"必看景点"和"推荐路线"两个key），而前端期望的是字符串，导致前端显示异常。在`generator.py`的JSON解析后添加完整的字段规范化逻辑：①`visit_highlights`如果是字典，转换为"必看景点：xxx、推荐路线：xxx"的字符串格式；②确保`legends`/`photo_spots`/`avoid_tips`是字符串列表（处理字符串、列表、其他类型的情况）；③确保`introduction`/`history`/`architecture`/`culture`是字符串类型。验证结果：所有字段类型正确，RAG服务正常工作（knowledge_chunks=4，从知识库检索到4个文档块） | 修改 `backend/app/services/rag/generator.py`（新增字段规范化逻辑） |
| 2026-08-31 | Bug修复 | **RAG服务启用+AI讲解功能完整修复**——在之前修复基础上完成RAG服务完整启用和验证：①在`.env`中添加`RAG_ENABLED=true`配置，启用RAG知识库服务；②修复`app/ai/__init__.py`缺少`chat_completion`导出的问题（`generator.py`通过`from ... import ai as ai_mod`导入包，需要在`__init__.py`中显式导出`chat_completion`函数）；③强制杀死所有旧的multiprocessing子进程（uvicorn reload模式下子进程可能运行旧代码），清除Python缓存后重新启动服务；④完整验证RAG服务：知识库29个景点、116个文档，Embedding模型`BAAI/bge-small-zh-v1.5`正常加载；⑤AI讲解接口测试通过：西湖景点返回完整的景点介绍、历史沿革、5条传说故事、3个拍照机位、5条避坑提示；⑥知识问答接口测试通过：返回详细的西湖必去景点介绍（断桥残雪、白堤、平湖秋月、苏堤、三潭印月、雷峰塔等），带1条来源引用 | 修改 `backend/.env`（新增RAG_ENABLED=true）、`backend/app/ai/__init__.py`（新增chat_completion导出） |
| 2026-08-31 | Bug修复 | **AI讲解功能报错修复+三层兜底机制**——用户反馈使用AI讲解时报错，排查发现3个根本原因：①`generator.py`调用了不存在的`ai_mod.chat_completion()`函数（`ai.py`中只有`chat_reply()`），在`ai.py`中新增`chat_completion()`底层LLM接口函数（接受messages列表、temperature、max_tokens、model参数，直接调用Ollama API，不包含业务逻辑），与高层的`chat_reply()`区分开；②`vector_store.py`和`knowledge_service.py`中使用`print()`输出日志，在uvicorn后台运行时stdout被关闭导致`BrokenPipeError: [Errno 32] Broken pipe`，将所有16个`print()`替换为`logger.warning/error/info()`；③`rag.py`中`get_rag_generator()`和`get_retriever()`调用在try-except块之外，初始化异常直接抛出导致500错误，为所有8个RAG接口（AI讲解/知识问答/卡片信息/知识查询/语义检索/导入景点/批量导入/删除知识/统计信息）添加完整的try-except兜底机制，当RAG服务不可用或初始化失败时返回通用介绍而非500错误。测试验证：所有接口返回code:0，RAG不可用时正确返回兜底结果，无新的错误日志 | 修改 `backend/app/ai/ai.py`（新增chat_completion函数）、`backend/app/services/rag/vector_store.py`（13个print→logger）、`backend/app/services/rag/knowledge_service.py`（3个print→logger）、`backend/app/api/rag.py`（8个接口添加try-except兜底） |
| 2026-08-30 | 代码风格优化 | **后端所有85个文件的代码风格统一优化全部完成**——参考caasm_server项目编码风格，按模块逐个文件规范化：①完善模块级文档字符串（功能特性+使用方式示例）；②优化类型注解（用Optional[X]/Callable/Dict[str, Any]/List[Dict[str, Any]]替代Any/裸dict/裸list）；③提取重复魔法值为模块级常量；④优化导入排序（标准库→第三方→本地分组并字母排序）；⑤优化代码结构和函数签名。按层推进：utils(3)→infrastructure(7)→security(5)→data(11)→services(34)→core(5)→ai(4)→api(16)，共85个文件全部通过py_compile语法检查和导入测试，大文件（planner.py/poi.py/daily_build.py等）仅优化导入和文档不改动函数实现确保安全 | backend/app/下所有85个Python文件（utils/3、infrastructure/7、security/5、data/11、services/34、core/5、ai/4、api/16） |
| 2026-08-29 | 规划引擎优化 | **4项核心优化全部落地（日志+缓存+去重+LLM提示词）**——针对用户提出的4个问题完成深度优化：①**景点池变化详细日志**：新增`_log_poi_pool`辅助函数，在规划的10个关键阶段输出景点池详细信息（枚举后/必打卡注入后/排除后/人群影响后/LLM优化前/LLM优化后/统一去重后/同名景区去重后等），包含景点名称、评分、类别、是否景区内子景点、LLM主题等字段，便于排查问题；②**POI检索缓存机制**：新增`poi_cache`数据库表（按行政区域+分类缓存，7天有效期）和`PoiCacheRepository`数据访问层（查询/保存/清理过期/统计），修改`search_pois`函数优先从数据库缓存检索，缓存不足时再去第三方检索，然后将结果存储到缓存，大幅减少第三方API调用；③**同名景区去重机制**：新增`dedup_duplicate_scenic_areas`函数，处理同一个景区的不同名称同时出现的问题（如"趵突泉景区"和"天下第一泉景区"、"故宫博物院"和"紫禁城"），采用3种去重策略：别名映射（维护常见景区别名表）、坐标距离（距离<500米视为同一景区，保留评分高的）、包含关系（名称包含另一个景区名称时保留更通用的），在规划流程中统一去重后调用；④**LLM优化提示词优化**：重写`OPTIMIZE_SYSTEM_PROMPT`，明确要求标志性景点必选（如北京的故宫/长城、济南的趵突泉/大明湖/千佛山），按优先级排序筛选原则（标志性景点>必去地标>评分高>用户偏好>类型均衡>地理合理>去重），增加常见错误避免提醒（不要遗漏标志性景点、不要因用户偏好排除核心景点、不要选同一景区的不同名称、不要选主景区和子景点），输出LLM原始结果日志便于排查。测试验证：济南2天情侣行程，所有阶段日志正常输出，同名景区去重生效，规划结果改善（第2天包含大明湖景区、五龙潭公园） | 新增 `backend/app/data/repositories/poi_cache_repository.py`（POI缓存数据访问层）；修改 `backend/app/data/database.py`（新增poi_cache表）、`backend/app/services/map/poi.py`（search_pois添加缓存逻辑）、`backend/app/core/planner.py`（新增_log_poi_pool辅助函数+dedup_duplicate_scenic_areas函数+10个阶段日志输出）、`backend/app/ai/ai.py`（重写OPTIMIZE_SYSTEM_PROMPT） |
| 2026-08-29 | 规划引擎优化 | **规则引擎深度集成（人群影响+体验曲线+行程检视）**——在创建规则引擎基础上，将核心规则能力深度集成到规划流程中：①景点筛选阶段应用人群影响规则，标记6种人群友好属性（亲子/老人/情侣/朋友/单人/家庭），人群友好景点评分+0.3提升优先级，情侣人群80%主要景点+20%辅助景点；②按天分配阶段应用体验曲线规则，第1天轻松入门/第2-3天核心景点/第4-5天中等强度/第6天+轻松收尾，计算每天实际强度评分和推荐强度，检查体力交替规则（连续核心景点建议中间安排轻松休息）；③规划完成后应用行程检视规则，8维度检视（景点数量/必去景点/人群匹配/时间合理/交通合理/预算合理/跨天去重/强度交替），不符合要求时从候选景点池补充；④出行方式影响计算，影响强度强/中/弱三级+规划程度详细/简要/不规划三级。修复方法调用错误（is_family_friendly接受poi dict参数而非name/category，get_recommended_intensity替代不存在的get_recommended_pace）。济南2天情侣行程测试通过：8个景点，总预算1240元，所有规则正常应用（group_influence_applied/experience_curve_applied/plan_review_issues/traffic_impact_calculated） | 修改 `backend/app/core/planner.py`（深度集成3大规则引擎+修复方法调用） |
| 2026-08-29 | 规划引擎重构 | **创建完整规则体系并落地实现**——基于《行程规划引擎完整规则体系（整合版）》文档，创建独立的规则引擎模块（rule_engine.py，约37KB），整合7大核心规则引擎：①ParameterNormalizer参数规范化器（时间/人数/预算的兜底和收束，最小1天/最大15天，最小1人/最大99人，4档预算）；②GroupInfluenceEngine人群影响引擎（6种人群类型的影响点和比例，单人90%/情侣80%/亲子60%/家庭85%/朋友70%/老人90%主要景点比例，亲子/老人友好景点筛选）；③TrafficFlexibilityEngine出行方式灵活引擎（影响强度强/中/弱三级，5维度评分；规划程度详细/简要/不规划三级，4维度评分；通行方式建议按距离/人群/预算）；④AttractionFilterEngine景点筛选引擎（同名地点处理、预算筛选、硬性约束、中国区域校验）；⑤DayAllocationEngine按天分配引擎（旅行体验曲线第1天轻松入门/第2-3天核心景点/第4-5天中等强度/第6天+轻松收尾，体力曲线高强度后必须低强度休息，景点强度分级高/中/低）；⑥PlanReviewEngine行程检视引擎（8维度检视：景点数量/必去景点/人群匹配/时间合理/交通合理/预算合理/跨天去重/强度交替，不符合时从候选景点池补充）；⑦PlanningRuleEngine主入口（统一调度所有规则引擎，单例模式）。同时修改planner.py集成规则引擎：规划开始前调用参数规范化，规划完成后调用行程检视与补充、出行方式影响强度计算。所有功能测试通过，Python语法检查通过 | 新增 `backend/app/core/rule_engine.py`（约37KB，7大规则引擎）；修改 `backend/app/core/planner.py`（集成规则引擎调用）；新增 `docs/行程规划引擎完整规则体系（整合版）.md`（约50KB，完整规则文档）；新增 `docs/行程规划引擎完整流程与规则体系.md`；新增 `docs/规划引擎核心规则体系设计.md` |
| 2026-08-29 | 数据架构优化 | **硬编码数据整合全部完成（前端+工具+清理）**——在后端整合基础上完成全部收尾：①创建前端字典数据管理面板（DictPanel.vue），支持必去景点、景点层级关系、配置常量、品牌文案4类数据的可视化CRUD，含分页、筛选、详情查看、缓存清除；②创建数据导入导出工具（scripts/export_dict_data.py），支持JSON格式的批量导入导出，可指定类型、覆盖模式；③补充44个大型景区的等级字段（5A/4A/世界遗产等），数据校验从44警告降为0警告0错误；④删除5个冗余硬编码数据文件（must_visit.py、poi_hierarchy.py、poi_hierarchy_extra.py、large_scenic_areas.py、famous_landmarks.py）及3个临时文件，共释放约95KB；⑤修复所有残留引用（planner.py、geocode.py、knowledge_service.py、admin_store.py注释），在dict_store中新增is_inside_large_scenic_area、get_scenic_area_weight_factor、get_all_poi_hierarchy三个函数；⑥后端服务正常启动，行程规划功能验证通过 | 新增 `frontend/src/views/admin/panels/DictPanel.vue`、`backend/scripts/export_dict_data.py`、`backend/scripts/fix_scenic_levels.py`；修改 `backend/app/dict_store.py`（新增3函数）、`backend/app/planner.py`、`backend/app/services/map/geocode.py`、`backend/app/services/rag/knowledge_service.py`、`backend/app/admin_store.py`、`frontend/src/views/admin/AdminApp.vue`（注册面板）；删除 `backend/app/data/must_visit.py`、`backend/app/data/poi_hierarchy.py`、`backend/app/data/poi_hierarchy_extra.py`、`backend/app/data/large_scenic_areas.py`、`backend/app/data/famous_landmarks.py` |
| 2026-08-29 | 数据架构优化 | **硬编码数据整合至数据库（全部推进完成）**——在第一阶段基础上完成全部优化：①修改planner_constants.py为动态配置版，优先从数据库读取配置，保留硬编码作为默认值和回退，提供get_xxx()函数支持动态更新；②创建后台管理接口（routers/admin_dict.py），提供必去景点、景点层级关系、配置常量、品牌文案4类数据的完整CRUD接口（列表/详情/创建/更新/删除），以及缓存清除和数据统计接口，更新后自动清除内存缓存；③创建数据校验工具（scripts/validate_dict_data.py），检查4类数据的完整性和一致性（必填字段、评分范围、JSON格式、重复数据等），校验结果：0错误，44警告（大型景区未填写等级）；④修复admin认证依赖导入错误；⑤后端服务正常启动，所有接口可用 | 新增 `backend/app/routers/admin_dict.py`、`backend/scripts/validate_dict_data.py`；修改 `backend/app/services/planner/planner_constants.py`（动态配置版）、`backend/app/main.py`（注册路由） |
| 2026-08-29 | 数据架构优化 | **硬编码数据整合至数据库**——用户反馈代码中有很多硬编码数据，希望整合至数据库。完成以下工作：①新增2张数据库表（poi_hierarchy景点层级关系表、dict_brand_copy品牌文案表）；②创建数据初始化脚本（scripts/init_dict_data.py），将4个硬编码数据文件（must_visit.py、poi_hierarchy.py、poi_hierarchy_extra.py、large_scenic_areas.py、famous_landmarks.py）合并导入数据库；③创建统一数据访问层（app/dict_store.py），提供必去景点、景点层级关系、大型景区识别、配置常量、品牌文案等查询接口，带内存缓存（TTL 300-600秒）；④修改planner.py和services/planner/must_visit.py，使用dict_store替代硬编码数据，兼容对象格式和字典格式；⑤数据导入结果：must_visit 289条、poi_hierarchy 75条、site_config 15条、dict_brand_copy 11条；⑥行程规划功能测试通过，济南2天行程正常生成 | 新增 `backend/app/dict_store.py`、`backend/scripts/init_dict_data.py`；修改 `backend/app/database.py`（新增2张表）、`backend/app/planner.py`（使用dict_store）、`backend/app/services/planner/must_visit.py`（使用dict_store） |
| 2026-08-29 | 规划引擎优化 | **新增两阶段规划策略（先空间整体路线规划，再时间拆分）**——用户希望规划时先忽略时间，按照出发点->目的城市(多个景点)->返程点安排出所有规划，然后按时间拆分。实现两阶段规划模块（`spatial_temporal_planner.py`）：第一阶段空间规划（筛选候选景点→最近邻贪心算法排序→形成完整游览路线→计算总距离/游览时间/交通时间），第二阶段时间拆分（根据天数/节奏/每天目标游览时间贪心拆分→住宿点作为第二天起点→最后一天装完剩余景点）。支持出发点/返程点/多城市路线，通过`plan_strategy`字段选择策略（traditional/spatial_temporal）。同时修复已有bug：`_daily_inspiration`名称错误（两处调用）。济南2天测试通过：7个景点路线（大明湖→五龙潭→趵突泉→泉城广场→黑虎泉...），拆分为2天，大明湖/趵突泉作为主景点含内部动线，子景点已过滤 | 新增 `backend/app/services/planner/spatial_temporal_planner.py`；修改 `backend/app/planner.py`（集成两阶段规划+修复_daily_inspiration bug）、`backend/app/models.py`（新增plan_strategy字段） |
| 2026-08-29 | 规划引擎优化 | **修复子景点作为独立点位排进规划的问题**——用户反馈济南行程中大明湖的子景点（超然楼、铁公祠、历下亭等）被排进规划，但大明湖主景点反而没被排进。实现子景点过滤机制：①在景点枚举阶段（`enumerate_scoped_attractions`）新增`_filter_inner_sub_pois`函数，从poi_hierarchy获取所有主景点的内部子景点名称集合；②如果候选POI的名称匹配某个主景点的内部子景点，且该主景点也在候选池中，则过滤掉子景点；③同时提升主景点的评分到至少4.8分并标记为must_visit，确保主景点被排进规划；④如果主景点不在候选池中，则保留子景点但标记is_sub_poi和parent_poi（降级处理）。覆盖28个主景点的126个内部子景点，测试用例验证通过（6个候选→过滤3个子景点→保留3个，主景点评分提升） | 修改 `backend/app/services/planner/geo_enum.py`（新增_filter_inner_sub_pois函数+子景点过滤逻辑） |
| 2026-08-29 | 文档建设 | **后续优化规划文档完成**——创建《AI旅行规划产品-后续优化规划文档》，系统梳理6大优化方向共20+具体优化项，按P1/P2/P3优先级分级，包含优化目标、问题背景、具体实现方案、涉及文件、预计工作量、预期效果、验收标准。P1优化（知识时效性标注+AI讲解前端体验优化）预计2周完成，P2优化（知识更新机制+用户画像前端体验优化）预计2-4周完成，P3优化（多模态知识+知识库持续扩展）长期推进。文档还包含执行路线图、数据质量标准、性能优化方向等内容，为后续迭代提供清晰指引 | 新增 `docs/optimization-roadmap.md`（约12KB，7大章节） |
| 2026-08-29 | RAG优化 | **知识库扩展至28个景区，覆盖17个城市**——在第一批18个景区基础上新增第二批10个热门景区：天坛（北京）、外滩（上海）、拙政园（苏州）、洪崖洞（重庆）、黄鹤楼（武汉）、龙门石窟（洛阳）、少林寺（郑州）、中央大街（哈尔滨）、橘子洲（长沙）、栈桥（青岛）。每个景区包含完整的三层POI数据（简介+内部动线4-5站+周边2处+避坑提示3条），向量库文档数从72增至112，本地POI数从18增至28，覆盖城市从11个增至17个。采用模块化数据结构（poi_hierarchy_extra.py），便于后续持续扩展。新景区知识查询全部验证通过 | 新增 `backend/app/data/poi_hierarchy_extra.py`（10个景区三层POI数据）；修改 `backend/app/data/poi_hierarchy.py`（导入合并补充数据） |
| 2026-08-29 | RAG优化 | **RAG景点名称模糊匹配功能上线**——解决用户输入景点名称与标准名不匹配的问题（如"故宫博物院"查不到知识）。实现四级匹配策略：①别名精确匹配（维护60+别名映射表，覆盖全称/简称/俗称，如"紫禁城"→"故宫"、"兵马俑"→"秦始皇兵马俑博物馆"）；②包含关系匹配（"杭州西湖"→"西湖"，长度差≤4）；③字符串相似度匹配（difflib编辑距离，cutoff=0.6）；④未匹配返回原名。所有RAG接口（知识查询/AI讲解/知识问答/语义检索/上下文生成）均自动应用名称标准化，匹配结果在元数据中标记matched_alias和standard_name。测试4个用例全部通过 | 修改 `backend/app/services/rag/retriever.py`（新增POI_ALIASES别名表+normalize_poi_name标准化函数+4级匹配策略） |
| 2026-08-29 | RAG依赖安装 | **RAG人文知识库服务完全就绪**——①依赖安装成功：chromadb 1.5.9 + sentence-transformers 6.0.0 + torch 2.13.0 + numpy 2.5.2；②模型下载成功：BAAI/bge-small-zh-v1.5（205M，中文轻量Embedding模型），使用hf-mirror国内镜像解决HuggingFace连接问题；③RAG服务启动成功：使用MPS Apple Silicon GPU加速，VectorStore初始化成功；④知识库批量导入成功：18个景区全部导入，72个向量化文档（每个景区4个分块：简介/内部动线/周边/避坑提示）；⑤功能验证通过：语义检索（查询"故宫的历史"返回3个相关结果）、点位卡片信息（西湖简介/最佳时间/避坑提示）、景点知识查询（故宫4个分块）全部正常。注意：后端启动需设置`HF_ENDPOINT=https://hf-mirror.com`环境变量，景点名称需与poi_hierarchy一致 | 修改 `backend/requirements.txt`（新增RAG依赖）、`backend/app/data/knowledge_base/`（向量库持久化数据） |
| 2026-08-29 | 前端功能集成 | **前端三大功能集成完成**——①点位卡片AI讲解：DayCard组件新增"✨ AI讲解"按钮，点击弹出完整讲解弹窗（8个模块：一句话定位/历史沿革/建筑格局/文化意义/名人典故/游玩重点/拍照机位/避坑提示），调用RAG API生成，支持加载态/错误态/无知识降级提示；②"我的"页面用户画像：新增"🎯 我的旅行偏好"卡片，展示8项偏好（节奏/预算/人群/交通/风格/美食/人文/拍照）+画像完整度+学习统计，登录后自动加载，越用越智能；③对话偏好自动学习：ChatPlanner在生成行程后自动调用learnFromPlan API学习用户偏好（已登录用户），静默执行不影响主流程。API封装新增32个接口（RAG 13个+用户画像8个+行程工作记忆11个） | 修改 `frontend/src/api/index.js`（新增32个API）、`frontend/src/components/DayCard.vue`（AI讲解按钮+弹窗+样式）、`frontend/src/views/Me.vue`（用户画像卡片+加载逻辑+样式）、`frontend/src/components/ChatPlanner.vue`（偏好自动学习） |
| 2026-08-29 | 全能力升级 | **《AI旅行规划产品｜全能力升级可落地执行计划》核心功能全部落地**——按最终版方案完成四阶段核心功能：①阶段一架构重构：行程工作记忆（session存储完整行程+参数+修改历史+用户偏好）、多轮增量修改（11个API+9种局部修改，不全局重刷）、引擎&LLM职责边界明确（编排器模块）、终极链路落地（LLM意图→引擎点位池→LLM编排→引擎二次校验，PlanValidator支持5类校验+自动修复）；②阶段二精细点位体系：三层POI数据结构（18个热门景区，126个内部子POI+60个周边附属POI，覆盖11城市）、POI模型扩展7个字段、引擎自动挂载内部动线+周边市井街区、前端DayCard展示升级（展开/收起内部路线+周边联动+避坑提示+5A徽章）；③阶段三人文内容体系：RAG服务完整框架（VectorStore向量存储+Retriever混合检索+RAGGenerator内容生成+KnowledgeService知识管理）、13个RAG API接口（知识查询/AI讲解/知识问答/检索/管理）、RAG知识库建设计划文档；④阶段四用户长期画像：UserProfileService（行为偏好自动学习+跨行程记忆带入+画像完整度计算）、8个用户画像API、从规划/修改行为中学习10+维度偏好。后端共新增4个服务模块+4个路由文件+约60个API接口，前端DayCard组件升级。RAG依赖（chromadb+sentence-transformers）后台安装中，安装完成后自动可用 | 新增 `backend/app/services/session/`（4文件）、`backend/app/services/orchestrator/`（3文件）、`backend/app/services/rag/`（5文件）、`backend/app/services/user_profile.py`、`backend/app/data/poi_hierarchy.py`、`backend/app/routers/itinerary.py`、`backend/app/routers/rag.py`、`backend/app/routers/profile.py`；修改 `backend/app/models.py`（POI扩展7字段）、`backend/app/planner.py`（三层POI自动挂载）、`backend/app/main.py`（注册4个新路由）、`frontend/src/components/DayCard.vue`（内部路线+周边联动展示） |
| 2026-08-29 | 精细点位 | **阶段二：精细点位体系完成**——按最终版方案落地阶段二核心功能：①三层POI数据结构（`backend/app/data/poi_hierarchy.py`）：18个热门景区完整数据（主POI/内部子POI/周边附属POI），覆盖11个城市，共126个内部子POI+60个周边附属POI，支持精确/别名/包含三种匹配方式；②POI模型扩展（`backend/app/models.py`）：新增inner_route/nearby_attractions/poi_level/recommended_duration/best_time/avoid_tips/has_hierarchy等7个扩展字段；③引擎自动挂载（`backend/app/planner.py`）：新增`_mount_poi_hierarchy`函数，行程生成后自动为主景点挂载内部游览动线和周边市井街区，大明湖示例：内部动线5个（超然楼→铁公祠→历下亭→北极阁→南丰祠）+周边附属4个（曲水亭街/芙蓉街/百花洲/宽厚里）。效果：行程从"景点跳转"升级为"真实游玩动线"，更贴合普通人真实旅行（逛景区+逛老街吃小吃） | 新增 `backend/app/data/poi_hierarchy.py`（18个景区三层POI数据，约26KB）；修改 `backend/app/models.py`（POI模型扩展7个字段）；修改 `backend/app/planner.py`（新增_mount_poi_hierarchy自动挂载函数） |
| 2026-08-29 | 架构重构 | **阶段一：核心架构重构完成**——按《AI旅行规划产品｜全能力升级可落地执行计划》落地阶段一全部4项任务：①行程工作记忆模块（`backend/app/services/session/`）：ItineraryWorkspace保存完整行程JSON+用户原始参数+修改历史+用户隐性偏好，SessionStore支持内存缓存+文件持久化+LRU淘汰，ModificationRecord记录每次修改快照；②多轮增量修改接口（11个API）：IncrementalEditor支持9种局部修改（节奏调整/增删POI/替换POI/加美食/去收费/主题调整/天数调整/重排顺序），所有修改只改对应区块不全局重刷，用户偏好自动沉淀；③引擎&LLM职责边界明确：创建编排器模块（`backend/app/services/orchestrator/`），文档化硬规则引擎（10项客观计算）与LLM语义层（5项创作理解）的职责边界；④终极链路落地：Orchestrator实现4阶段链路（LLM意图解析→引擎生成点位池→LLM编排→引擎二次校验），PlanValidator支持5类校验（体量/时间轴/去重/住宿/POI完整性）+自动修复。效果：行程逻辑性提升，支持持续对话迭代，彻底解决AI地理幻觉，为后续阶段奠定架构基础 | 新增 `backend/app/services/session/`（itinerary_workspace.py/session_store.py/incremental_editor.py/__init__.py）；新增 `backend/app/services/orchestrator/`（pipeline.py/validators.py/__init__.py）；新增 `backend/app/routers/itinerary.py`（11个API）；修改 `backend/app/main.py`（注册itinerary路由） |
| 2026-08-29 | 文档 | **完善景点人文RAG知识库建设与落地计划**：基于《AI旅行规划产品｜全能力升级可落地执行计划》补充完善RAG专项规划，覆盖十大模块——RAG在三大系统（规划引擎/LLM/RAG）中的定位与协同、知识库建设方案（7级数据源优先级、4阶段覆盖范围50→350→4000→10000景点、完整JSON数据结构含history/architecture/culture/visit_guide等、按主题分块策略）、技术选型（Chroma向量库+bge-small-zh Embedding+向量/BM25混合检索）、四大应用场景（AI景点完整讲解/点位卡片人文信息自动填充/行程人文短句/多轮对话知识问答）、四阶段落地计划（MVP 2周→阶段一3周→阶段二4周→阶段三持续）、数据更新与维护策略（月度/季度/半年更新频率+版本管理+质量保障）、与现有系统集成方案（后端模块结构+3张数据库表设计+6组API接口+规划引擎集成点）、质量评估指标（知识库/检索/生成三类指标）、风险应对、Top50热门景点清单。同时在README添加「相关文档」索引 | 新增 `docs/rag-knowledge-base-plan.md`（约16KB，十大模块完整规划）；修改 `README.md`（相关文档索引+变更记录） |
| 2026-08-28 | 规划优化 | **修复大型景区与景区内小景点重复出现的问题**：修改 `backend/app/data/large_scenic_areas.py` 的 `is_large_scenic_area` 函数，添加更严格的匹配逻辑（排除包含楼/亭/阁/殿等景区内小景点关键词的名称、包含匹配需以景区/风景区/公园/博物院等后缀结尾）；修改 `backend/app/planner.py` 的去重逻辑，提取大型景区的基础名称（如"大明湖景区"->"大明湖"），用基础名称匹配景区内小景点；在 LLM 优化路径和统一去重路径都添加了去重逻辑。效果：大型景区（如大明湖景区）作为整体规划点位，景区内小景点（如大明湖超然楼、大明湖历下亭）被过滤掉，不再重复出现。已验证：去重前5个点位→去重后3个点位（保留大明湖景区、趵突泉、芙蓉街，过滤大明湖超然楼、大明湖历下亭） | 修改 `backend/app/data/large_scenic_areas.py`（is_large_scenic_area匹配逻辑）；修改 `backend/app/planner.py`（去重逻辑优化） |
| 2026-08-28 | 规划优化 | **大型景区识别与景区内小景点优先级降低**：新增 `backend/app/data/large_scenic_areas.py` 大型景区识别数据（65个大型景区，含大明湖、西湖、故宫、颐和园、兵马俑、鼓浪屿等），提供 `is_large_scenic_area`/`is_inside_large_scenic_area`/`get_scenic_area_weight_factor` 三个函数；修改 `backend/app/planner.py`，在枚举景点后添加大型景区识别和权重标记（景区内小景点权重降低为0.3，标记 `is_scenic_inner=True`）；修改 LLM 优化后的筛选逻辑，补充景点时优先选择非景区内小景点（排序优先级：非景区内小景点 > 评分高 > 权重高）。效果：大型景区（如大明湖）作为整体规划点位，景区内小景点（如大明湖超然楼）优先级降低，不单独参与跨景区路径规划 | 新增 `backend/app/data/large_scenic_areas.py`（65个大型景区）；修改 `backend/app/planner.py`（大型景区识别+筛选逻辑优化） |
| 2026-08-28 | Bug修复 | **修复用户明确指定行政区域时仍被知名景点知识库覆盖的问题**：修改 `backend/app/services/map/geocode.py`，当输入包含明确行政区域关键词（市/省/自治区/地区/自治州/的）时跳过知名景点坐标知识库，尊重用户指定；修改 `backend/app/data/famous_landmarks.py` 的 `lookup_famous_landmark` 函数，移除反向匹配条件（`clean_key in clean_name`），避免"乌鲁木齐大明湖"错误匹配到"大明湖"。已验证：大明湖→济南（知识库）、乌鲁木齐大明湖→新疆（地图API）、沈阳故宫→沈阳（地图API），全部正确 | 修改 `backend/app/services/map/geocode.py`（添加行政区域判断）；修改 `backend/app/data/famous_landmarks.py`（移除反向匹配） |
| 2026-08-28 | LLM优化 | **LLM意图分析增加行政区域推断能力**：修改 `backend/app/ai.py` 的 `INTENT_SYSTEM_PROMPT`，要求 LLM 在识别景点名时利用世界知识推断所属城市，返回完整地名格式为"城市名+景点名"（如"北京市圆明园""济南市大明湖"），从源头减少同名地点歧义。已验证：圆明园→北京市圆明园、大明湖→济南市大明湖、西湖→杭州市西湖、兵马俑→西安市兵马俑、鼓浪屿→厦门市鼓浪屿，全部正确 | 修改 `backend/app/ai.py`（INTENT_SYSTEM_PROMPT第4条规则） |
| 2026-08-28 | Bug修复 | **修复同名地点歧义导致规划地点飘逸的问题**：新增 `backend/app/data/famous_landmarks.py` 知名景点坐标知识库（37个知名景点，含大明湖、西湖、故宫、天安门、长城、兵马俑、鼓浪屿等），提供 `lookup_famous_landmark()` 函数支持精确匹配和模糊匹配；修改 `backend/app/services/map/geocode.py` 的 `geocode()` 函数，在检索优先级最前面（第0步）添加知名景点坐标知识库检索，优先返回正确的景点坐标和所属城市，避免地图 API 将"大明湖"错误解析到新疆乌鲁木齐。已验证：大明湖→济南市（山东省）、西湖→杭州市（浙江省）、故宫→北京市，source=famous_landmark | 新增 `backend/app/data/famous_landmarks.py`（5942字节，37个景点）；修改 `backend/app/services/map/geocode.py`（添加知名景点检索） |
| 2026-08-28 | 前端优化 | **移除行程数据本地缓存，改用 keep-alive + 全局状态**：新增 `frontend/src/store/index.js` 轻量级全局状态存储（Vue reactive），提供 setCurrentPlan/getCurrentPlan/clearCurrentPlan 方法，行程数据在内存中传递不写入本地存储；修改 `App.vue` 添加 `<keep-alive :include="['Home','Explore','Me']">` 包裹 router-view，缓存对话/探索/我的页面状态，行程页不缓存（每次读取最新全局状态）；修改 `ChatPlanner.vue` 的 openPlan 函数，用 `setCurrentPlan(plan)` 替代 `sessionStorage.setItem`；修改 `Plan.vue` 的 onMounted，用 `getCurrentPlan()` 替代 `sessionStorage.getItem`，无行程数据时返回对话页；给 `Home.vue`/`Explore.vue`/`Me.vue` 添加 `defineOptions({ name })` 确保 keep-alive include 匹配生效。解决了新旧行程数据混淆问题（如规划大明湖却显示新疆行程） | 新增 `frontend/src/store/index.js`（713字节）；修改 `frontend/src/App.vue`（keep-alive）；修改 `frontend/src/components/ChatPlanner.vue`（移除sessionStorage）；修改 `frontend/src/views/Plan.vue`（移除sessionStorage）；修改 `frontend/src/views/Home.vue`/`Explore.vue`/`Me.vue`（添加name） |
| 2026-08-28 | Bug修复 | 修复 `backend/app/routers/trips.py` 中地理编码失败时 `geo` 为 None 导致 `TypeError: 'NoneType' object is not subscriptable` 的问题，添加 None 检查，地理编码失败时使用默认坐标 | 修改 `backend/app/routers/trips.py` |
| 2026-08-28 | 架构优化 | **Phase 3 可观测性完成**：新增 `app/metrics.py` 轻量级指标收集模块（无外部依赖），支持 Counter/Gauge/Histogram 三种指标类型，提供 MetricsRegistry 注册表和全局 metrics 实例；预定义 http_requests_total/http_requests_errors_total/http_request_duration_seconds/http_active_requests 四个请求指标；提供 MetricsMiddleware 中间件自动收集请求指标（按方法/路径/状态码）；提供 /metrics 接口，支持 Prometheus 文本格式（?format=prometheus，默认）和 JSON 格式（?format=json）输出，可直接被 Prometheus 抓取或用于运维监控；在 `app/main.py` 注册 MetricsMiddleware 中间件和 /metrics 接口。链路追踪（trace_id 贯穿）和结构化日志（JSON格式+敏感字段脱敏+按天轮转）已在之前实现。已验证：指标模块导入正常、指标记录和输出功能正常、FastAPI 应用启动正常 | 新增 `backend/app/metrics.py`（10151字节）；修改 `backend/app/main.py`（注册指标中间件+ /metrics接口） |
| 2026-08-28 | 架构优化 | **Phase 2 高可用能力（前3项完成）**：①Redis 分布式缓存（新增 `app/redis_client.py`，提供连接池/读写/删除/统计/清空等操作，支持连接超时/密码/DB选择/Key前缀；修改 `app/cache.py` 支持双后端：Redis 优先 + 内存兜底，读写双写，读取优先 Redis 未命中再读内存，Redis 不可用时自动回退内存；在 `app/config.py` 添加 REDIS_ENABLED/REDIS_HOST/REDIS_PORT/REDIS_PASSWORD/REDIS_DB/REDIS_MAX_CONNECTIONS 等配置项；安装 redis 8.1.0 库）；②熔断器（新增 `app/circuit_breaker.py`，轻量级实现无外部依赖，支持 CLOSED/OPEN/HALF_OPEN 三态状态机，连续失败达阈值打开熔断器，冷却时间后进入半开探测，探测成功关闭/失败重新打开；提供 CircuitBreaker 类、@circuit_breaker 装饰器、get_circuit_breaker 全局单例、get_all_stats 统计接口；可用于保护高德/腾讯/AI 等第三方 API 调用，避免故障雪崩）；③全局限流（新增 `app/rate_limiter.py`，滑动窗口算法内存实现，支持 IP 级限流（默认120次/分钟）和用户级限流（默认60次/分钟），考虑 X-Forwarded-For/X-Real-IP 反向代理，从 JWT token 解析用户 ID，限流命中返回 429 + X-RateLimit-* 响应头，跳过健康检查/文档/静态资源路径；在 `app/main.py` 注册 RateLimiterMiddleware 中间件）。已验证：所有模块导入正常、功能测试通过、FastAPI 应用启动正常。Phase 2.4 Celery 异步任务队列待后续实现（需 Redis/RabbitMQ 作为 broker） | 新增 `backend/app/redis_client.py`（5925字节）、`backend/app/circuit_breaker.py`（7900字节）、`backend/app/rate_limiter.py`（5964字节）；修改 `backend/app/cache.py`（双后端支持）、`backend/app/config.py`（Redis配置）、`backend/app/main.py`（注册限流中间件） |
| 2026-08-28 | 架构优化 | **Phase 1 生产基础能力全部完成**：①统一异常处理（新增 `app/exceptions.py`，定义 ErrorCode 错误码枚举（1xxxx通用/2xxxx认证/3xxxx业务/4xxxx第三方/5xxxx数据）、AppException 基类及 AuthException/BusinessException/ThirdPartyException/DataException 快捷类、register_exception_handlers 全局异常处理器（处理 AppException/RequestValidationError/HTTPException/Exception 四类异常，统一响应格式，不泄露堆栈）；②统一响应格式（新增 `app/responses.py`，定义 ApiResponse<T> 泛型响应类、PaginatedData 分页包装、ok/fail/paginated 快捷函数，响应格式统一为 {code,message,data,trace_id}）；③健康检查接口（新增 `app/health.py`，提供 /health 轻量级检查（应用自身）和 /health/detailed 详细检查（数据库/缓存/地图服务/AI服务），用于容器探针和负载均衡；④数据库连接池可配置（在 `app/config.py` 新增 DB_POOL_MAX_CONNECTIONS/DB_POOL_MIN_CACHED/DB_POOL_MAX_CACHED/DB_POOL_MAX_SHARED/DB_POOL_BLOCKING/DB_CONNECT_TIMEOUT/DB_READ_TIMEOUT/DB_WRITE_TIMEOUT 配置项，修改 `app/database.py` 使用可配置参数）；⑤多环境配置支持（在 `app/config.py` 添加 ENV 环境变量支持 dev/staging/prod，按优先级加载 .env.{ENV} > .env，新增 env/debug/log_level/log_format 配置项，不同环境默认值不同）。已验证：所有模块导入正常、配置正确、FastAPI 应用启动正常 | 新增 `backend/app/exceptions.py`（7283字节）、`backend/app/responses.py`（2608字节）、`backend/app/health.py`（3228字节）；修改 `backend/app/main.py`（注册异常处理器+健康检查路由）、`backend/app/config.py`（连接池+多环境配置）、`backend/app/database.py`（可配置连接池） |
| 2026-08-28 | Bug修复 | **修复规划接口 NameError: _departure_message 未定义**：Phase 3 拆分 planner.py 时，`departure_message` 函数已迁移到 `services/planner/utils.py`（不带下划线前缀），但 planner.py 中仍调用 `_departure_message`（带下划线）且未导入该函数。修复：①在 planner.py 导入中添加 `departure_message, daily_inspiration, pick` 函数；②将调用从 `_departure_message` 改为 `departure_message`。验证：规划接口调用成功，返回完整行程规划数据（3207字节），出发宣言功能正常（"你要去的北京，正等着你的脚步。出发吧，勇气都在路上。"） | 修改 `backend/app/planner.py`（导入+调用修复） |
| 2026-08-28 | 架构重构 | **Phase 4 amap.py 拆分完成：5个模块+兼容层，amap.py 从1273行瘦身到70行（减少94%）**：①新建 `app/services/map/` 目录，拆分为5个模块：client.py（基础客户端20+函数/常量：HTTP请求、Provider探测、距离计算、城市坐标、MOCK_POIS演示数据）、geocode.py（地理编码3个函数：地名→坐标五级检索+同名消歧）、poi.py（POI搜索15+函数：按类别/行政分级/就近/周边探索/点位解析/酒店+内部辅助函数）、route.py（路径规划3个函数：两点间交通方案五种模式+均速估算兜底）、weather.py（天气2个函数：天气查询高德优先→腾讯兜底）；②更新 `app/services/map/__init__.py` 统一导出所有函数；③将 `app/amap.py` 改为兼容层（70行），从新模块重新导出所有函数，保持向后兼容；④修复 MOCK_POIS 缺失问题（添加到 client.py）；⑤已验证：amap.py 兼容层导入正常、services.map 新模块导入正常、FastAPI 应用启动正常（55个路由）、函数行为正确（haversine/provider/infer_category等）。Phase 4 累计拆分5个模块，amap.py 从1273行减少到70行（减少94%） | 新增 `backend/app/services/map/client.py`、`backend/app/services/map/geocode.py`、`backend/app/services/map/poi.py`（25825字节）、`backend/app/services/map/route.py`、`backend/app/services/map/weather.py`、`backend/app/services/map/__init__.py`；修改 `backend/app/amap.py`（1273行→70行） |
| 2026-08-28 | 架构重构 | **Phase 4 amap.py 拆分（进行中）：路径规划+天气模块迁移到 services/map/route.py 和 weather.py**：①新建 `app/services/map/route.py`——迁移3个路径规划函数（route两点间交通方案缓存30分钟、route_tencent腾讯direction接口、route_impl路径规划实现自驾/公共交通/骑行/步行/混合五种模式含均速估算兜底）；②新建 `app/services/map/weather.py`——迁移2个天气函数（weather天气查询缓存2小时高德优先→腾讯兜底、weather_tips天气出行提示）；③两个模块都从 client.py/geocode.py 导入基础函数，避免重复代码；④已验证：route.py 和 weather.py 导入正常，weather_tips 函数行为正确。Phase 4 累计已迁移4个模块（client.py+geocode.py+route.py+weather.py，共28+函数），amap.py 剩余约15个POI搜索相关函数待迁移（search_pois/search_attractions/search_around/explore_around/resolve_poi/search_hotel_near等） | 新增 `backend/app/services/map/route.py`（4664字节，3个函数）、`backend/app/services/map/weather.py`（2451字节，2个函数） |
| 2026-08-28 | 架构重构 | **Phase 4 amap.py 拆分（进行中）：地理编码模块迁移到 services/map/geocode.py**：①新建 `app/services/map/geocode.py`——迁移3个地理编码函数（load_place_geo懒加载内置地名坐标表、lookup_place_geo内置地名坐标查询地级市/省/精确区县/区县->所属地级市、geocode地名->坐标含本地库→缓存→高德→腾讯→内置兜底五级检索和同名消歧）；②geocode.py 从 client.py 导入基础函数（provider/amap_get/tencent_get/AMAP_BASE/TENCENT_BASE/CITY_CENTERS），避免重复代码；③已验证：geocode.py 导入正常，lookup_place_geo('北京') 返回正确坐标。Phase 4 累计已迁移 client.py（20+基础函数）+ geocode.py（3个地理编码函数），amap.py 剩余约25个函数待迁移（POI搜索/路径规划/天气/周边探索等） | 新增 `backend/app/services/map/geocode.py`（6368字节，3个函数） |
| 2026-08-28 | 架构重构 | **Phase 4 amap.py 拆分（进行中）：基础客户端模块迁移到 services/map/client.py**：①新建 `app/services/map/` 目录（含 `__init__.py`）；②新建 `app/services/map/client.py`——迁移基础客户端功能（AMAP_BASE/TENCENT_BASE API地址常量、CITY_CENTERS 25个城市中心坐标、GENERIC_POIS通用点位池、haversine球面距离计算、log_slow慢请求日志、_http HTTP客户端、amap_sem全局信号量并发限制、amap_get高德GET请求含重试、tencent_sign腾讯签名MD5、tencent_get腾讯GET请求含签名、provider当前Provider探测、probe_amap高德Key探测、probe_tencent腾讯Key探测）；③client.py 自包含所有依赖（从 cache/config/logger 导入），避免循环导入。已验证：client.py 导入正常，haversine/provider 函数行为正确。注：amap.py 剩余函数（geocode地理编码、search_pois POI搜索、search_attractions景点搜索、search_around周边搜索、explore_around周边探索、resolve_poi点位解析、search_hotel_near酒店搜索、route路径规划、weather天气）待后续逐步迁移到 geocode.py/poi.py/route.py/weather.py/explore.py | 新增 `backend/app/services/map/__init__.py`、`backend/app/services/map/client.py`（6966字节，20+函数/常量） |
| 2026-08-28 | 架构重构 | **Phase 3 planner.py 拆分完成：核心规划函数迁移到 daily_build.py，planner.py 从1680行瘦身到541行（减少68%）**：①在 `app/services/planner/daily_build.py` 中新增12个核心规划函数（build_items构建PlanItem+站间交通、build_geo_day构建一天行程、geo_plan规则化地理规划主入口、pick_new_focus挑选新主区域、rebuild_day_from_region按区域重建一天、replace_strays替换跨区点位、enforce_region_distinct跨天地域去重、apply_timeline时间轴校验、rule_item规则点位、day_budget单日预算、total_breakdown总预算拆分、date_label日期标签）；②daily_build.py 自包含所有依赖（从 utils/cluster/planner_constants 导入），避免循环导入；③planner.py 改为从 daily_build.py 导入这12个函数，删除重复的函数定义。效果：planner.py 从904行进一步减少到541行（累计减少1139行，68%），核心规划逻辑独立可测试。已验证：planner 和 daily_build 导入正常，函数行为一致。注：剩余函数（build_plan主入口、_generate、_build_long_span_summary、规则降级函数_rule_plan/_pick_pool/_prioritize/_fallback_pois/_emergency_plan/_ensure_days）待后续 Phase 4 迁移到 engine.py 和 rule_fallback.py | 改 `backend/app/services/planner/daily_build.py`（新增12个核心函数，从234行增加到572行）；改 `backend/app/planner.py`（删除12个重复核心函数，减少363行） |
| 2026-08-28 | 架构重构 | **Phase 3 planner.py 拆分（进行中）：逐日构建模块迁移到 services/planner/daily_build.py**：①新建 `app/services/planner/daily_build.py`——迁移10个逐日构建函数（reorder_day同日内地理重排、pick_auxiliary选就近辅助点、local_aux远处区域就地检索、dedup_landmark_day同天地标去重、pick_hotel就近选住宿、province_areas省级按市分隔、region_of点位区域标签、day_focus一天主区域、match_pool_by_name按名称匹配点位池、balance_day品类均衡+补足）；②daily_build.py 自包含_rank/_is_closed/_is_landmark辅助函数，避免循环依赖；③planner.py 改为从 daily_build.py 导入，删除重复的10个函数定义及辅助函数（_rank/_avg_rank/_is_closed/_is_landmark/_drop_sub_venues）。效果：planner.py 从1167行进一步减少到904行（累计减少776行，46%），逐日构建逻辑独立可测试。已验证：planner 和 daily_build 导入正常，函数行为一致。注：核心函数（build_geo_day/geo_plan/pick_new_focus/rebuild_day_from_region/replace_strays/enforce_region_distinct）因依赖复杂暂保留在 planner.py，待后续逐步迁移 | 新增 `backend/app/services/planner/daily_build.py`；改 `backend/app/planner.py`（删除10个重复函数+5个辅助函数，减少263行） |
| 2026-08-28 | 架构重构 | **Phase 3 planner.py 拆分（进行中）：必去地标注入模块迁移到 services/planner/must_visit.py**：①新建 `app/services/planner/must_visit.py`——迁移 inject_must_visit_attrs 函数（把「必打卡」知名地标从 must_visit 知识库解析后注入主线景点池并置前，含地图 API 失败时的城市中心坐标近似 POI 兜底、同名地标补打 must_visit/hot 权重与评分）；②must_visit.py 自包含 _is_closed 辅助函数，避免循环依赖；③planner.py 改为从 must_visit.py 导入，删除重复的函数定义。效果：planner.py 从1235行进一步减少到1167行（累计减少513行，30%），必去地标注入逻辑独立可测试。已验证：planner 和 must_visit 导入正常，函数行为一致 | 新增 `backend/app/services/planner/must_visit.py`；改 `backend/app/planner.py`（删除1个重复函数，减少68行） |
| 2026-08-28 | 架构重构 | **Phase 3 planner.py 拆分（进行中）：地理聚类模块迁移到 services/planner/cluster.py**：①新建 `app/services/planner/cluster.py`——迁移8个地理聚类函数（cluster_attractions景点邻近聚类、merge_sparse_clusters稀疏簇合并、make_area区域构造、greedy_group贪心分组高分锚点+就近高分、split_to_balance区域拆分均衡、dist_tier距离分层市区/近郊/远郊、assign_areas_to_days区域分配到天由近及远按圈层轮流、area_label区域主题名）；②cluster.py 自包含_rank/_avg_rank/_is_closed/_is_landmark辅助函数，避免循环依赖；③planner.py 改为从 cluster.py 导入，删除重复的8个聚类函数定义。效果：planner.py 从1414行进一步减少到1235行（累计减少445行），聚类逻辑独立可测试。已验证：planner 和 cluster 导入正常，函数行为一致 | 新增 `backend/app/services/planner/cluster.py`；改 `backend/app/planner.py`（删除8个重复聚类函数，减少179行） |
| 2026-08-28 | 架构重构 | **Phase 3 planner.py 拆分（进行中）：地理枚举模块迁移到 services/planner/geo_enum.py**：①新建 `app/services/planner/geo_enum.py`——迁移8个地理枚举函数（is_in_china中国境内判断、load_province_best_season省份最佳季节知识库、resolve_province目的地→省份解析、months_str月份列表格式化、is_province省级判断、scope_attrs按行政编码检索景点、enumerate_scoped_attractions三级枚举含跨城污染过滤）；②新建 `app/services/planner/planner_constants.py`——迁移规划引擎常量（SCOPE_KEYWORDS行政分级关键词、CLUSTER_MAX_DIST聚类距离、SCOPE_MAX_RANGE范围上限、CLOSED_MARKERS闭馆标记、AUX_FOOD_BAD_NAMES美食护栏、LANDMARK_WORDS 74个全国知名地标特征词）；③planner.py 改为从 geo_enum.py 和 planner_constants.py 导入，删除重复的函数定义和常量定义。效果：planner.py 从1680行减少到1414行（减少266行），地理枚举逻辑独立可测试。已验证：planner 和 geo_enum 导入正常，函数行为一致 | 新增 `backend/app/services/planner/geo_enum.py`、`backend/app/services/planner/planner_constants.py`；改 `backend/app/planner.py`（删除8个重复函数+7组常量，减少266行） |
| 2026-08-28 | 架构重构 | **Phase 3 planner.py 拆分（进行中）：常量迁移到 services/planner/utils.py**：①新建 `app/services/planner/` 目录（含 `__init__.py`）；②新建 `app/services/planner/utils.py`——迁移规划引擎常量（SLOT_ORDER/SLOT_TIME/BUDGET_TABLE/PACE_ITEMS/GROUP_PACE/DEPARTURE_MESSAGES/DAILY_INSPIRATIONS）和简化版工具函数（pick/departure_message/daily_inspiration/is_closed/is_landmark/rank/avg_rank/drop_sub_venues）；③planner.py 改为从 utils.py 导入常量，删除重复的常量定义（从1680行减少约50行）。注：planner.py 中的工具函数实现更复杂（使用 CLOSED_MARKERS/LANDMARK_WORDS 常量、type字段判断），暂保留原实现，待后续逐步迁移。已验证：planner 导入正常，常量与 utils.py 一致 | 新增 `backend/app/services/__init__.py`、`backend/app/services/planner/__init__.py`、`backend/app/services/planner/utils.py`；改 `backend/app/planner.py`（常量导入） |
| 2026-08-28 | 架构重构 | **Phase 2 路由拆分：main.py 从564行瘦身到180行，业务路由按模块拆分**：①新建 `app/routers/` 目录，按模块拆分6个路由文件——`auth.py`（注册/登录/检查用户名/me，4个路由）、`plan.py`（一键规划/对话规划/路线，3个路由，含LLM意图分析辅助函数_llm_chat_plan和天气降级_try_weather_reply）、`explore.py`（POI搜索/周边探索，2个路由）、`trips.py`（记录足迹/查看足迹，2个路由）、`conversations.py`（会话列表/流/获取/保存/删除，5个路由）、`deps.py`（共享依赖：鉴权require_user/bearer_user、请求模型AuthRequest/TripReport/ConversationSave、常量HOME_CENTER）；②main.py 瘦身后仅保留 app 创建、中间件（CORS/安全头/请求日志）、lifespan、3个公开接口（/、/api/status、/api/site/config），通过 include_router 注册所有业务路由；③修复 FastAPI 版本兼容问题——0.141.1版本 include_router 异常（路由不注册），降级到 0.115.0（starlette 0.38.6）后正常。已验证：55个路由全部正确注册，status/plan鉴权/登录功能正常 | 新增 `backend/app/routers/__init__.py`、`backend/app/routers/deps.py`、`backend/app/routers/auth.py`、`backend/app/routers/plan.py`、`backend/app/routers/explore.py`、`backend/app/routers/trips.py`、`backend/app/routers/conversations.py`；改 `backend/app/main.py`（从564行瘦身到180行）；依赖降级 fastapi 0.141.1→0.115.0、starlette 1.6.0→0.38.6 |
| 2026-08-28 | 架构重构 | **Phase 1 数据库层重构：统一数据库连接 + 表结构全面升级**：①新建 `app/database.py`——统一数据库连接管理（pymysql 连接池 PooledDB，回落直连）、8张表结构定义（users/trips/conversations/admins/must_visit/site_config/generation_log/login_log）、init_db() 幂等初始化、execute_query/execute_update 统一执行函数；②表结构全面升级——经纬度 DOUBLE→DECIMAL(10,6)、rating DOUBLE→DECIMAL(3,1)、distance_km→DECIMAL(10,2)、所有表增加 created_at/updated_at TIMESTAMP、users 增加 status/last_login_at、conversations 增加 summary/message_count/deleted_at、admins 增加 last_login_at、must_visit 增加 adcode/lng/lat、site_config.v→TEXT+description、generation_log.detail→TEXT+user_id+params；③新增 login_log 登录审计日志表（user_id/username/client_ip/status/failure_reason）；④增加查询索引（idx_trips_created/idx_convo_updated/idx_mv_category/idx_gen_user 等）；⑤新建 `scripts/migrate_db_schema.py`——幂等迁移脚本（information_schema 检查字段是否存在，已存在跳过），为已存在的表添加新字段和索引；⑥main.py 改用 database.init_db() 替代 userstore.init_db()。已验证：后端导入正常，8张表全部升级成功 | 新增 `backend/app/database.py`、`backend/scripts/migrate_db_schema.py`；改 `backend/app/main.py`（改用 database.init_db） |
| 2026-08-28 | 安全加固 | **P0 安全加固：SQL注入防护 + 登录失败限流 + 安全响应头 + CORS收紧**：①新建 `app/db_security.py`——SQL 注入防护工具集（字段白名单校验 validate_field、排序方向校验、LIMIT/OFFSET 强转 safe_limit/safe_offset/safe_page、LIKE 参数化 like_param、安全 WHERE 构造 build_where、安全 UPDATE SET 构造 build_update_set），所有动态 SQL 必须经过本模块校验；②新建 `app/login_security.py`——登录失败限流（同用户名5次失败锁定15分钟、同IP10次失败锁定30分钟），线程安全内存字典实现，含 check_login_locked/record_login_failure/record_login_success/get_login_status 接口；③登录接口集成限流——`POST /api/auth/login` 增加 Request 参数获取客户端IP，登录前检查锁定状态，失败记录计数并返回剩余尝试次数，成功重置计数，锁定时返回 429；④新建 `app/security_middleware.py`——安全响应头中间件（X-Content-Type-Options/X-Frame-Options/X-XSS-Protection/Referrer-Policy/Permissions-Policy/HSTS）+ 错误信息脱敏（sanitize_error_message 正则脱敏密码/token/key/secret）；⑤CORS 配置收紧——默认从 `*` 改为仅允许本地开发地址（127.0.0.1:5173/localhost:5173等），allow_methods 限制为 GET/POST/PUT/DELETE/OPTIONS，allow_headers 限制为 Content-Type/Authorization/X-Trace-Id；⑥生产环境通过 CORS_ORIGINS 环境变量配置具体域名。已验证：安全响应头全部生效，连续5次登录失败后账号锁定15分钟，第6次被拒绝 | 新增 `backend/app/db_security.py`、`backend/app/login_security.py`、`backend/app/security_middleware.py`；改 `backend/app/main.py`（登录接口集成限流+安全中间件+CORS收紧）、`backend/app/config.py`（CORS默认值收紧） |
| 2026-08-28 | 安全鉴权 | **全局登录鉴权：所有核心功能必须登录后才能使用**：①后端——给 `POST /api/plan`、`POST /api/chat/plan`、`POST /api/route`、`POST /api/poi/search`、`POST /api/explore`、`POST /api/trips`、`GET /api/trips` 全部加上 `_require_user` 鉴权，未登录返回 401；公开接口（/api/status、/api/site/config、/api/auth/*）保持无需登录；②前端 API 封装——核心接口（createPlan/chatPlan/getRoute/exploreAround/poiSearch/reportTrip/getTrips/对话历史）改为 `authedRequest` 自动从 localStorage 取 token，401 响应自动清除登录态；③前端路由守卫——`router.beforeEach` 拦截所有 `meta.requiresAuth` 路由（/、/plan、/explore），未登录跳转到 `/me?redirect=原路径&auth=login`；④Me.vue——检测到 redirect/auth 参数时自动打开登录弹窗，登录成功后跳回原页面，已登录带 redirect 时直接跳转。已验证：未登录访问 plan/chat 返回 401，登录后正常访问 | 改 `backend/app/main.py`（7个核心接口加鉴权）、`frontend/src/api/index.js`（authedRequest自动带token+401清登录态）、`frontend/src/router/index.js`（路由守卫）、`frontend/src/views/Me.vue`（自动开弹窗+登录后跳转）、`frontend/src/utils/user.js`（API签名更新）、`frontend/src/utils/convo.js`（API签名更新） |
| 2026-08-28 | 数据优化 | **阿里云 DataV 全量行政区域数据导入：adcode 从 39 提升到 6364（100%覆盖）**：新建 `scripts/import_datav_geo.py`——从阿里云 DataV（https://datav.aliyun.com/portal/school/atlas/area_selector）异步并发拉取全国省→市→区县三级 GeoJSON 数据（34省+475市+2728区县，共3237个有adcode条目），生成 `admin_divisions.json`（version=datav-2026，6326个by_name条目，30组同名地点）。数据包含完整的 name/adcode/level/parent/lng/lat，geocode 全部从本地命中（source=local），不再依赖第三方 API。旧文件自动备份为 `.bak` | 新增 `backend/scripts/import_datav_geo.py`；改 `backend/app/data/admin_divisions.json`（全量替换为DataV数据） |
| 2026-08-28 | 规划引擎 | **同名地点消歧：行政区域+景点同名检测与上下文优先**：①`geo_local.py` 数据结构升级——by_name 支持同名地点存储为 list（如北京朝阳区/长春朝阳区），新增 `_entries_at` 辅助函数兼容 dict/list；②新增 `lookup_all(name)`（查找所有同名候选）、`is_ambiguous(name)`（检测同名歧义）、`lookup_with_context(name, context_province, context_city)`（上下文优先消歧，有上下文但无匹配时返回 None 走第三方）；③`amap.geocode` 增加 context_province/context_city 参数，本地检索用 lookup_with_context，无法消歧时标记 ambiguous=True + candidates 列表；④`amap.resolve_poi` 增加同名检测——搜索结果中有多个同名景点时标记 ambiguous，增加 center 参数按距离排序优先最近的；⑤`save_entry` 支持同名地点合并为 list；⑥`stats()` 增加 ambiguous_names/total_entries 统计；⑦新建 `scripts/supplement_ambiguous_districts.py`——补充 36 条知名同名地点（北京朝阳区/南京鼓楼区/杭州西湖区/天津和平区等，含 adcode），with_adcode 从 2 提升到 39。已验证：朝阳(ctx=北京)→北京朝阳区、鼓楼(ctx=南京)→南京鼓楼区、鼓楼(ctx=福州)→福州鼓楼区，全部正确消歧 | 新增 `backend/scripts/supplement_ambiguous_districts.py`；改 `backend/app/geo_local.py`（list 存储+消歧函数）、`backend/app/amap.py`（geocode/resolve_poi 上下文消歧+同名标记）、`backend/app/data/admin_divisions.json`（补充36条知名同名地点） |
| 2026-08-28 | 数据修复 | **行政区域数据关联关系修复：市级 parent 从 0% 提升到 100%**：①新建 `scripts/fix_admin_relations.py`——用「最近省份中心点法」补充省→市映射（658条市级全部补充 parent），修复区县 parent 缺失（46条，重庆/香港的区），修复 parent=自己的省直辖县级市（34条，济源/仙桃/东莞等改为所属省+标记 direct_control），修复 level=1 异常数据（香港/台北改为 province）；②修复后统计：市级 parent 覆盖率 0%→100%，区县级 99.2%→100%，省直辖标记 34 条，with_adcode 2 条（香港/台北，其余待全量更新补充）；③验证：杭州→浙江、成都→四川、西安→陕西、东莞→广东(省直辖)、济源→河南(省直辖)、慈溪→宁波→浙江（多级追溯）、渝北→重庆，全部正确；④`_resolve_province` 现在可通过本地行政区域库向上追溯到省级，不再完全依赖硬编码城市映射 | 新增 `backend/scripts/fix_admin_relations.py`；改 `backend/app/data/admin_divisions.json`（关联关系修复，version=1.1-fixed-relations） |
| 2026-08-28 | 规划引擎 | **大时间跨度归一化与收束：月/年级按省/月合理分配**：①`models.py` PlanRequest 新增 `span_unit`（day/week/month/year）和 `span_value`（原始数量），不再只看收束后的天数；②`intent.py` 新增 `_extract_span(text)` 提取原始跨度（不做收束），`_rule_extract` 同步设置 span_unit/span_value，DEFAULT_PARAMS 新增字段；③`main.py` 构建 PlanRequest 时传入 span_unit/span_value；④新建 `data/province_best_season.json`——34个省级行政区最佳出行月份知识库（如云南3-5月/10-11月、海南11-3月、黑龙江6-8月/12-2月）；⑤`planner.py` 新增 `_load_province_best_season`/`_resolve_province`（geo.province→本地行政区域库向上追溯→300+常见城市→省映射）/`_build_long_span_summary`（月级1个月=1省、2个月=2省；年级12个月每月一省按最佳出行季节分配）/`_months_str`（月份范围格式化）；⑥build_plan 大跨度时生成 `span_summary` 并返回 PlanResponse.span_summary 字段；⑦`models.py` PlanResponse 新增 span_summary 字段。已验证：杭州1个月→浙江省（最佳3-5月/9-11月），2个月→浙江省+上海市，1年→12个月按季节分配（冬季冰雪/温暖、春季江南、夏季避暑、秋季秋景） | 新增 `backend/app/data/province_best_season.json`；改 `backend/app/models.py`（PlanRequest+PlanResponse 新增跨度字段）、`backend/app/intent.py`（_extract_span+_rule_extract）、`backend/app/main.py`（PlanRequest 传入跨度）、`backend/app/planner.py`（大时间跨度月度分配逻辑） |
| 2026-08-28 | 规则校验 | **中国区域规划限制：仅规划中国境内目的地**：①`planner.py` 新增 `_is_in_china(geo)` 函数——三级判断（本地命中→province 非空→经纬度范围 73°-135.5°E/17.5°-54°N，含港澳台及南海诸岛）；②`build_plan` 在 geocode 后插入中国区域校验，境外目的地返回 `source=rejected` + 友好提示（"暂不支持中国以外目的地，足迹先从祖国大好河山开始"）；③修复 `amap.geocode` 兜底逻辑——无法识别的地名返回 None（不再返回假的北京中心坐标，避免境外目的地被误判为中国）；④`build_plan` 新增 geo=None 处理，提示"无法识别目的地，请检查地名"。已验证：北京/杭州/香港/台北正常规划，东京/纽约/巴黎被拒绝 | 改 `backend/app/planner.py`（_is_in_china + build_plan 校验 + geo=None 处理）、`backend/app/amap.py`（geocode 兜底返回 None） |
| 2026-08-28 | 架构优化 | **行政区域数据本地化：geocode 优先本地检索，减少第三方依赖**：①新建 `app/data/admin_divisions.json`（6221 条，含省/市/区县，支持别名，从 place_geo.json 转换生成）；②新建 `app/geo_local.py`——封装本地检索（lookup 支持别名/简称、lookup_by_adcode、get_children）+ 增量写入（save_entry，第三方命中后自动回写）+ 统计（stats）；③改造 `amap.geocode`——检索优先级改为「本地行政区域库 → 缓存 → 高德 → 腾讯 → 兜底」，本地命中直接返回（source=local），第三方命中后自动增量回写本地；④新建 `scripts/convert_admin_divisions.py`——从 place_geo.json 转换生成初始 admin_divisions.json；⑤新建 `scripts/update_admin_divisions.py`——全量更新脚本，从高德 district_query 拉取全国省→市→区县三级（含 adcode），支持 --dry-run，自动备份旧版本；⑥后台新增 `GET /api/admin/geo/stats`（查看数据统计）和 `POST /api/admin/geo/refresh`（触发全量更新）。已验证：北京/杭州/慈溪/东城区/济南 geocode 全部 source=local，零第三方调用；端到端杭州行程正常，LLM 优化主题"江南水乡"+智能时长生效 | 新增 `backend/app/geo_local.py`、`backend/app/data/admin_divisions.json`、`backend/scripts/convert_admin_divisions.py`、`backend/scripts/update_admin_divisions.py`；改 `backend/app/amap.py`（geocode 优先本地+增量回写）、`backend/app/admin_router.py`（geo 管理接口） |
| 2026-08-28 | 规划引擎 | **LLM 行程优化（P0 三件套：智能筛选+主题分组+智能时长）**：①`ai.py` 新增 `optimize_itinerary(req, attrs)`——LLM 从候选景点中筛选最适合用户的景点（days×4），分配主题标签（如"皇城根脉""皇家园林""长城雄关"）、建议时长（大型景点240-360min/中型120-180min/小型60-90min）、最佳时段（上午/下午/晚上），输出 JSON；②`planner.py` build_plan 在 `_geo_plan` 前调用 LLM 优化，用 LLM 选中的景点替换全量 attrs（不足时补充 hot 景点），注入 `llm_duration`/`llm_theme`/`llm_best_slot` 字段；③`_build_geo_day` 时长优先用 LLM 建议（替代固定150min），时段优先用 LLM 建议（替代固定上午）；④DayPlan.theme 优先用 LLM 主题标签（替代"区域·经典一日"）；⑤`_inject_must_visit_attrs` 修复地图 API 配额耗尽时 resolve_poi 全失败导致 attrs 为空的问题——失败时用城市中心坐标构建近似 POI（标记 approximate），确保 must_visit 地标能进入候选池、LLM 优化可正常运行，地图 API 恢复后自动用真实坐标。已验证：北京3天→LLM精选故宫(240min,上午)/天安门(60min,中午)/颐和园(180min,下午)，主题"皇城根脉"，LLM 做决策、规则引擎做执行 | 改 `backend/app/ai.py`（optimize_itinerary+OPTIMIZE_SYSTEM_PROMPT）、`backend/app/planner.py`（build_plan接入LLM优化+_build_geo_day用LLM时长/时段+DayPlan.theme用LLM主题+_inject_must_visit_attrs近似坐标兜底） |
| 2026-08-28 | Bug修复 | **LLM 意图分析多轮上下文污染 + 非法 intent 值修复**：①`INTENT_SYSTEM_PROMPT` 中 `"intent": "weather | plan | poi | chat"` 的 `|` 符号误导 LLM 输出组合值（如 `"weather|plan"`），改为示例值 `"plan"` 并明确"只能取四个值之一，严禁组合"；②强化"只分析最后一条用户消息，完全忽略之前消息的内容/地点/话题"（修复上一轮聊德州天气、这一轮规划大明湖被带偏成德州的问题）；③新增规则：plan 意图提到具体景点名（大明湖/西湖/故宫）时直接填该景点名作为 destination（后端自动定位所属城市），不要替换成其他城市；④`_llm_chat_plan` 增加 intent 容错：LLM 输出非法值时从 args 推断（有 destination→plan，有 city→weather，否则 chat）。实测带历史上下文「德州天气→帮我规划大明湖」→ intent=plan, destination=大明湖，正确 | `backend/app/ai.py`（INTENT_SYSTEM_PROMPT 强化约束）、`backend/app/main.py`（_llm_chat_plan intent 容错） |
| 2026-08-28 | 可观测性 | **服务端结构化日志监控（M10）**：①新增 `app/logger.py`——JSON 格式日志（ts/level/logger/msg/额外字段）+ 控制台人类可读输出 + 文件按天轮转保留 7 天 + 敏感字段（密码/token/key）自动脱敏 + `read_recent_logs` 后台查询；②`main.py` 新增请求日志中间件（trace_id 贯穿 + method/path/status/duration_ms/client_ip，4xx=WARNING/5xx=ERROR，异常自动记录堆栈）；③`amap.py` 地图 API 统一入口加失败/慢请求日志（>3s 记 WARNING，失败记 status/error）；④`planner.py` build_plan 加入口/出口日志（目的地/天数/点位数/预算/数据源/AI模型/耗时）；⑤`ai.py` analyze_intent 加成功/解析失败/异常日志；⑥后台新增 `GET /api/admin/logs`（按级别/关键词过滤 + 级别统计）+ 前端「日志监控」面板（级别筛选/关键词搜索/级别统计/彩色日志列表/异常堆栈展示）。已验证：日志文件生成、请求记录、后台查询均正常 | 新增 `backend/app/logger.py`、`frontend/src/views/admin/panels/LogPanel.vue`；改 `backend/app/main.py`（请求中间件+logger导入）、`backend/app/amap.py`（API失败/慢请求日志）、`backend/app/planner.py`（规划入口/出口日志）、`backend/app/ai.py`（意图分析日志）、`backend/app/admin_router.py`（/logs接口）、`frontend/src/views/admin/AdminApp.vue`（注册日志面板） |
| 2026-08-28 | Bug修复 | **天气咨询城市提取不依赖手写城市表**：`_extract_destination` 新增天气咨询句式匹配（"X的天气/气温/温度/冷不冷/热不热/下雨/下雪/降温/几度/会不会冷/穿衣/防晒/台风/雾霾/穿什么/冷吗/热吗/多少度"），直接提取 X 作为城市并加停用词过滤（今天/明天/昨天/现在/这里/那里/最近/本地/全国等），修复"德州的天气如何"因德州不在 `CITY_CENTERS` 手写有限城市表中被识别为空、AI 回复"先告诉我城市名"的问题；实测德州/济南/北京/上海/杭州/成都/三亚/哈尔滨均正确识别，停用词正确过滤为空 | `backend/app/intent.py`（`_extract_destination` 新增天气句式分支+停用词） |
| 2026-08-28 | 兼容修复 | **腾讯签名鉴权 + 探测策略修复**：①腾讯 WebService API 强制签名鉴权，新增 `TENCENT_SK`（签名密钥）配置与 `_t_sign`（MD5 签名：参数名 ASCII 排序→`完整URL路径?query+sk`，含 `/ws` 前缀，用 `urlsplit(url).path` 提取）——此前 `url.replace(TENCENT_BASE,"")` 误删 `/ws` 导致签名永远验签失败（status 111）；②`probe_amap`/`probe_tencent` 改为**优先探测规划主能力 POI 检索**（高德 place/text、腾讯 place/search），避免 geocoder 配额耗尽/通过造成的误判（高德 POI 超限应判不可用切腾讯、腾讯 geocoder 超限但 place 可用仍判可用）；③腾讯 Key 未配 sk 时 `_t_get` 诚实返回 None 降级。实测：真实腾讯 key+sk 签名通过（place/search 返回真实 POI），腾讯 geocoder/place 返回 status 121（该 key 每日配额已耗尽）时全部接口诚实降级不崩 | `backend/app/amap.py`（`_t_sign`+`_t_get` 签名+双 probe 主能力探测）、`backend/app/config.py`（tencent_sk）、`backend/.env`、`backend/.env.example`、`README.md` |
| 2026-08-28 | 兼容方案 | **腾讯地图 API 兼容层**（高德额度耗尽兜底）：后端新增腾讯地图 WebService Provider——①`config.py` 新增 `TENCENT_KEY`（环境变量+后台 `site_config` 持久化双来源）、`set_tencent_key`/`tencent_ready`/`tencent_key_source`；②`amap.py` 新增 `_t_get`（腾讯 status=0 判活）、`probe_tencent()` 启动真实探测、`provider()` 三态判定（**高德优先 → 腾讯兜底 → mock 诚实降级**，高德不可用自动切腾讯真实数据）；③全部数据接口加腾讯平替：地理编码 `/geocoder/v1`、POI 关键词检索 `/place/v1/search`（region/nearby 两种边界）、行政分级景点枚举、地标解析、酒店就近检索、路径规划 `/direction/v1`（驾车/公交/步行/骑行）、天气 `/weather/v1`；缓存键带 provider 前缀防切换污染；④腾讯 POI 无评分/价格→中性占位 0（不编造），排序依赖召回顺序；⑤`_is_province` 兼容腾讯 6 位 adcode（省级以 0000 结尾）；⑥后台 M8「高德与缓存」面板新增**腾讯 Key 管理**（`POST /api/admin/tencent/key` 持久化+探测+换 Key 清缓存，`GET /api/admin/amap` 返回双 provider 状态含 `provider`/`tencent_*` 字段），前端 AmapPanel 双 Key 配置 UI；⑦`/api/status` 新增 `provider`/`tencent_ready`/`tencent_mode`。已实测：假 Key 全部接口诚实降级不崩、腾讯模式 build_plan 端到端跑通、后台腾讯 Key 保存/探测/清空正常 | 新增 `backend/app/` 无新文件（扩展已有）；`backend/app/config.py`、`backend/app/amap.py`、`backend/app/planner.py`、`backend/app/main.py`、`backend/app/admin_router.py`、`backend/app/admin_store.py`、`frontend/src/views/admin/panels/AmapPanel.vue`、`backend/.env`、`backend/.env.example`、`README.md` |
| 2026-08-28 | 规划引擎 | **意图参数检测规范化 + 规划能力规则落地**：①输入参数补齐——新增出发点 `origin`、人数 `travelers`（默认1）、返回点 `return_point`，出行方式 `traffic_mode` 扩展为 自驾/公共交通/骑行/步行/混合（默认公共交通，`_route_impl` 新增骑行/步行分支）；②时间跨度年/月/日归一化收束（`_normalize_days` 支持中文数字：一周→7、一个月→30→收束15、两年→365→收束15，MAX_DAYS=15；`build_plan` 按实际可分配区域/市数再收束，避免规划点不足或过于庞杂）；③**省级目的地按市分隔规划**（`_province_areas`：全省景点按 cityname 分组→市内聚类→每天一个市，实测山东4天=泰山/曲阜/周村/欧乐堡）；④**住宿锚点**（`amap.search_hotel_near` 就近选酒店，写入 `DayPlan.hotel`，`_reorder_day` 以次日起点=前晚住宿点重排，前端 DayCard 展示「当晚住宿」）；⑤修复**直辖市误判为省**（北京/上海 level='省' 但 adcode=110000 等，`_is_province` 用直辖市 adcode 排除，修复北京3天被收束成1天）；⑥`_inject_must_visit_attrs` 过滤闭馆地标（修复杭州出现「西湖(暂停开放)」）；⑦must_visit 注入提前到 build_plan，时间跨度收束基于完整景点池 | `backend/app/models.py`、`backend/app/intent.py`、`backend/app/main.py`、`backend/app/planner.py`、`backend/app/amap.py`、`frontend/src/components/DayCard.vue` |
| 2026-08-28 | 文档 | **景点规划引擎规则梳理**：逐行梳理 `planner.py` 规划规则——主流程、行政分级枚举、必去地标注入、聚类、圈层分配、单日构建、跨天去重、兜底引擎、预算节奏参数与可调点速查（含全部阈值），沉淀为可查询文档 | 新增 `docs/planner-rules.md` |
| 2026-08-28 | 文档 | **代码目录结构梳理**：按模块梳理后端（入口/配置/存储/业务核心/后台管理/基础设施/数据资产）与前端（骨架/三板块视图/组件/工具/管理后台）代码目录，产出结构总览、模块职责表、调用关系速览与常用命令，便于定位与上手 | 新增 `docs/code-structure.md` |
| 2026-08-28 | 后台功能 | **高德 API Key 后台管理**（M8 扩展）：后台「高德与缓存」面板新增 API Key 管理（仅超级管理员）——①`settings.amap_key` 改为可运行时动态覆盖（后台配置优先、环境变量兜底）；②新增 `POST /api/admin/amap/key`：Key 持久化到 `site_config` 表、运行时立即生效、保存后自动 `probe_amap()` 探测有效性、换 Key 自动清空旧缓存（旧 Key 缓存不可信）；③`GET /api/admin/amap` 新增 `key_source`（site/env/none）与 `key_masked`（掩码显示）；④启动 lifespan 从 site_config 恢复后台保存的 Key，实现持久化；⑤C 端公开 `GET /api/site/config` 与后台配置读取默认剔除 `amap_key` 敏感键。已实测：无效 Key→探测失败进入演示模式；真实 Key→探测通过进入真实模式；清空→回退环境变量。修复前端动态组件传 `role` prop 与 HTML 原生属性冲突（改用 `userRole`） | `backend/app/config.py`（`set_amap_key`/`amap_key_source`/amap_key 改 property）、`backend/app/admin_router.py`（新增 `/amap/key`+amap 状态扩展）、`backend/app/admin_store.py`（`get_config` 加 include_secret）、`backend/app/main.py`（lifespan 恢复后台 Key）、`frontend/src/views/admin/AdminApp.vue`、`frontend/src/views/admin/panels/AmapPanel.vue`（Key 管理 UI+userRole） |
| 2026-08-28 | 后台功能 | **管理后台 9 大模块全部落地**（M1-M9）：①M1 登录与权限——`admins` 表+独立 JWT（scope=admin）+super/ops 两级角色+`/api/admin/*` 统一守卫（未登录 401、非 super 访问账号管理 403）；初始超级管理员 `admin01/Admin@123456`；②M2 仪表盘——用户/新增/行程/足迹城市/会话/消息统计+运行状态；③M3 用户管理——`users.enabled` 列、列表(足迹/行程/对话数)、详情、启停用（停用即拒登录）；④M4 行程足迹——行程列表、Top 城市分布、平均天数；⑤M5 对话运营——会话列表/详情（消息逐条）、含行程/纯聊天统计；⑥M6 内容与景点——`must_visit` 建表并从 py 知识库种子导入，planner 改为**表优先读取**（后台增删必去地标后新行程即时生效）、城市库接口；⑦M7 AI 管理——模型状态/当前生效模型+`generation_log` 生成日志（chat_plan 落库）；⑧M8 高德与缓存——Key/模式状态、缓存按类型统计与一键清空、TTL 展示；⑨M9 系统配置——`site_config` 表维护品牌文案+TTL，TTL 通过 `cache.set_ttl/get_ttl` **保存后对新缓存立即生效**，品牌文案经公开接口 `GET /api/site/config` 下发、Home.vue 改为接口渲染（后台改文案 C 端首页即时更新）。前端新增 `/admin` 独立后台（登录页+侧边栏 9 模块面板）。已全接口 + 浏览器实测：登录/401/403、增删地标、改 slogan 同步首页、启停用户、缓存清空 | 新增 `backend/app/admin_store.py`、`backend/app/admin_router.py`；`backend/app/userstore.py`（admins/must_visit/site_config/generation_log 建表+users.enabled+登录鉴权校验）、`backend/app/cache.py`（set_ttl/get_ttl）、`backend/app/amap.py`（TTL 动态读取）、`backend/app/planner.py`（must_visit 表优先）、`backend/app/main.py`（挂载 admin 路由+site/config+AI 日志）、`frontend/src/router/index.js`（/admin 路由）、`frontend/src/App.vue`（admin 隐藏 tabbar）、`frontend/src/api/index.js`（adminRequest/getSiteConfig）、`frontend/src/views/Home.vue`（品牌文案接口化）、新增 `frontend/src/views/admin/`（AdminLogin+AdminApp+9 面板） |
| 2026-08-28 | 账号体系 | **用户名占用实时检查**：注册 tab 用户名失焦即调用后端 `GET /api/auth/check-username` 检查是否被占用（不区分大小写），即时提示「用户名可用 / 已被占用 / 长度不合法」，避免提交时才报错；注册提交时后端仍以唯一索引强制兜底防重复 | `backend/app/userstore.py`（新增 `username_exists`）、`backend/app/main.py`（新增 `/api/auth/check-username`）、`frontend/src/api/index.js`（checkUsername）、`frontend/src/views/Me.vue`（blur 实时检查+状态提示） |
| 2026-08-28 | 账号体系 | **注册规则完善**：①「我的」板块未登录态移除「注册」按钮，注册入口收敛到登录弹窗内的 tab（登录页体现注册）；②用户名限 6~20 字符；③密码 8~64 字符、需包含大写/小写/数字/特殊字符中至少三种、不能与用户名相同/是其倒序/包含超过两个连续的用户名字符；前后端双重校验（前端即时提示+后端强制），后端返回具体错误文案，旧用户登录不受影响 | `frontend/src/views/Me.vue`（去注册按钮+注册tab提示+前端 validateCredentials）、`backend/app/userstore.py`（新增 `validate_credentials`、register 返回错误详情）、`backend/app/main.py`（register 接口透传错误） |
| 2026-08-28 | 后端修复 | **修复历史污染导致的目的地/天数错乱**：多轮对话里 AI 参数提取被历史旧地点带偏（例：先聊杭州行程+济南天气，再说「帮我规划济南3天两夜」被误生成杭州2天）。改为**规则对最新一条用户消息命中的强信号（目的地/天数/人群/预算/风格/节奏/交通/兴趣）优先于 AI**，历史仅兜底；`_rule_extract` 拆分最新消息优先逻辑（`_latest_hits` 标记），天数支持「3天两夜→3」；AI prompt 强化「最后一条消息最权威，忽略历史旧地点」。实测带历史场景：destination=济南、days=3，正确生成济南3天行程 | `backend/app/intent.py`（`_rule_extract` latest 优先 + `_latest_hits` + `_match_kw_opt` + prompt 强化） |
| 2026-08-28 | 后端功能 | **天气咨询接入真实数据**：chat 意图中识别天气类提问（天气/气温/下雨/降温/冷不冷/穿衣等），调用高德实时天气接口（TTL 2h）返回真实数据文案（城市/天气/气温/风力/温馨提示），不再让 AI 泛泛而谈；非天气咨询仍走 AI 普通对话。实测「济南的天气如何」→ 雾·22℃·西北风≤3级；「北京今天会下雨吗」→ 阵雨18℃带伞提示 | `backend/app/main.py`（`_try_weather_reply` 天气分支）、`backend/app/intent.py`（新增 `extract_city` 公开函数） |
| 2026-08-28 | 后端功能 | **对话阶段区分意图**：`/api/chat/plan` 先判断用户意图——`plan`（规划/调整行程：去X玩N天、改预算/天数/节奏/风格、增删景点）→ 生成行程；`chat`（普通咨询/闲聊：天气/美食/门票/交通/最佳时间/行程咨询/寒暄）→ 走本地 AI 普通对话回复，**不再每次对话都规划行程**。意图只依据**最新一条用户消息**规则判断（chat 强信号优先、默认 chat 不打断规划），避免被整段历史规划上下文污染（修复「北京天气怎么样」被历史带偏误生成杭州行程）；参数仍从全对话提取（AI 优先+规则兜底）。新增 `ai.chat_reply`（无 JSON 结构普通对话）。实测：天气/美食/人均/闲聊/行程咨询→chat 无行程；带历史调预算/加景点/去景点→plan 生成行程 | `backend/app/intent.py`（intent 字段+`_rule_classify` 最新消息判断+信号表）、`backend/app/ai.py`（chat_reply）、`backend/app/main.py`（chat_plan 意图分支） |
| 2026-08-28 | 前端精简 | **取消手动填写，规划完全由对话提供**：移除 Home 的「📝手动填写」tab 与整套表单（目的地/天数/人群/预算/风格/节奏/交通/兴趣），规划入口收敛为纯对话（ChatPlanner 默认唯一展示）；相应移除表单逻辑（form/submit/toggleStyle/toggleInterest）与 constants 导入。后端 `/api/plan`（createPlan）接口保留未删，便于后续复用 | `frontend/src/views/Home.vue`（重写为纯对话板块） |
| 2026-08-28 | 前端重构 | **历史会话并入统一消息流**：取消「📚历史会话」按钮面板与「新对话」的 tab 式区隔，所有会话（当前+历史）消息按时间倒序合并进同一个对话框展示（每个会话带标题+日期分隔头，最新/当前会话在底部可继续对话）；向上滚动到顶部自动加载更早的历史会话（`/api/conversations/stream` before_id 游标分页，每批 8 条）。发送时只带当前会话消息做多轮上下文（修复历史会话污染 AI 意图解析）；修复 buildStream/loadOlder 对 const ref 漏写 `.value` 导致的 "Assignment to constant variable" | 新增 `backend/app/main.py` `/api/conversations/stream`、`backend/app/userstore.py` `get_conversation_stream`；改 `frontend/src/api/index.js`（conversationStream）、`frontend/src/utils/convo.js`（fetchConvoStream）、重写 `frontend/src/components/ChatPlanner.vue`（统一消息流） |
| 2026-08-28 | 前端优化 | **历史会话改时间线**：历史面板改为按日期分组（今天/昨天/日期）时间线排列，默认加载最新一批（含当天会话），面板滚动到底部自动上拉加载更早的历史（before_id 游标分页，每批 20 条），到底显示提示；每条显示标题/消息数/时间。实测：首屏 20 条→上拉追加至 27 条全量。同时修复并发重复建会话 bug（persistNow 改 promise 链串行化） | `backend/app/userstore.py`（list_conversations 加 before_id/limit 分页）、`backend/app/main.py`（/api/conversations 加参数）、`frontend/src/api/index.js`（分页参数）、`frontend/src/utils/convo.js`（fetchConvoList 透传参数）、`frontend/src/components/ChatPlanner.vue`（时间线分组/上拉加载/串行化保存） |
| 2026-08-28 | 功能新增 | **历史会话**：①登录用户按用户存储（MySQL 新增 conversations 表，messages JSON 含行程 plan，按 user_id 隔离）；②未登录会话存 localStorage，页面切换/刷新后保留；③登录后自动把本地会话同步到后端（restoreOrSync），随后走后端读写；④对话板块新增「📚历史会话」面板（列表/加载/删除）与「🆕新对话」。修复两个竞态：AI 回复生成后立即保存（防切页丢失）、仅开场白时不再创建空会话、会话标题自动取首条用户消息 | 新增 `frontend/src/utils/convo.js`；改 `backend/app/userstore.py`（conversations 表 + CRUD）、`backend/app/main.py`（/api/conversations GET/POST/DELETE）、`frontend/src/components/ChatPlanner.vue`（持久化+历史面板）、`frontend/src/api/index.js`（会话接口）、`frontend/src/utils/user.js`（notifyUserChanged 登录态广播）、`frontend/src/views/Me.vue`（登录/登出发广播） |
| 2026-08-28 | 前端修复 | **默认路由兜底**：新增 catch-all 路由 `/:pathMatch(.*)* → redirect /`，未匹配路径一律重定向到默认对话板块，避免访问未知路径白屏。实测 `#/no-such-page-xyz` 自动落回 `#/` | `frontend/src/router/index.js` |
| 2026-08-28 | 前端重构 | **三板块 Tab 化**：底部固定 TabBar（💬对话 / 🧭探索 / 👤我的），对话为默认板块（保留对话/手动双模式与品牌 hero），探索板块独立成 Tab（去掉"返回"按钮改由 Tab 导航接管），新增「我的」板块收纳登录/注册弹窗、用户卡、足迹统计、最近行程、能力入口（周边探索/对话规划/数据来源）与退出登录；未登录展示"点亮足迹"品牌引导卡；行程页 `/plan` 全屏隐藏 TabBar。规划后足迹上报广播依旧生效，「我的」板块即时刷新 | 新增 `frontend/src/views/Me.vue`；重写 `frontend/src/App.vue`（TabBar 布局）、`frontend/src/views/Home.vue`（纯对话板块）、`frontend/src/router/index.js`（加 `/me` 路由）、`frontend/src/views/Explore.vue`（去返回按钮） |
| 2026-08-28 | 架构升级 | **账号体系生产化**：①存储 **SQLite → MySQL**（`travel_app` 库、专用 `travel` 账号、utf8mb4/InnoDB/外键级联）；②会话 **自研 token → JWT**（HS256 + `exp` 过期，默认 24h，过期/伪造签名一律拒绝，无状态不落库）；③密码 **sha256+盐 → bcrypt**（cost 12，注册即 bcrypt，旧 sha256 用户登录通过后自动升级重哈希）；④提供迁移脚本 `scripts/migrate_sqlite_to_mysql.py`（SQLite 用户与出行记录迁入 MySQL，实测 traveler + 5 条足迹迁移成功）；⑤`.env` 新增 `DB_*`/`JWT_*` 配置项，`requirements.txt` 新增 bcrypt/PyJWT/pymysql；前端 token 存取对 JWT 透明零改动。端到端实测：注册→登录（返回 JWT）→对话规划→足迹走 MySQL 即时 +1，未登录 401 | 重写 `backend/app/userstore.py`；`backend/app/config.py`、`backend/app/main.py`、`backend/requirements.txt`、`backend/.env`；新增 `backend/scripts/migrate_sqlite_to_mysql.py` |
| 2026-08-28 | Bug修复 | **足迹上报 422 修复**：`api/index.js` 的 `request()` 中 `{ headers, ...options }` spread 顺序错误，导致带 `Authorization` 的 POST 请求 `options.headers` 覆盖合并后的 `Content-Type`，后端收到原始 bytes 而校验失败（`model_attributes_type` 422）——表现为"登录后对话/手动规划能生成行程但出行记录写不进去"。改为 `{ ...options, headers }` 后上报正常，足迹即时 +1 | `frontend/src/api/index.js` |
| 2026-08-28 | 新功能 | **用户概念（方向三）**：新增登录/注册（加盐 SHA-256 密码哈希 + token 会话，SQLite 存储）与出行记录能力——登录用户每次规划成功（对话/手动）自动上报一次出行（目的地/天数/风格/到参考出发地的探索里程），未登录**不记录**；首页新增"我的足迹"卡片（点亮城市数 / 出发次数 / 探索里程 + 最近城市），支持退出登录、上报后即时刷新；未登录展示"点亮你的足迹"引导 | 新增 `backend/app/userstore.py`、`frontend/src/utils/user.js`；`backend/app/main.py`、`frontend/src/api/index.js`、`frontend/src/views/Home.vue`、`frontend/src/components/ChatPlanner.vue` |
| 2026-08-28 | 新功能 | **品牌气质注入对话模型（方向一）**：AI SYSTEM_PROMPT 人格化为"去见山海"旅行伙伴（温暖有力量、鼓励出发）；对话开场白、缺目的地追问、生成成功回复全部品牌化（"世界在等你，XX见！…剩下的，交给你的脚步"）；首页 hero 更名"去见山海 · 世界很大，替你踏平第一步"，footer/placeholder 同步品牌化 | `backend/app/ai.py`、`backend/app/main.py`、`frontend/src/views/Home.vue`、`frontend/src/components/ChatPlanner.vue` |
| 2026-08-28 | 新功能 | **旅程叙事（方向二）**：行程结果注入出发宣言 `departure_message`（"去见山海"式一句话点燃出发勇气）与每日寄语 `inspiration`（按当日主题/天数生成），行程页顶部展示出发宣言、每日卡片顶部展示当日寄语，呼应"激发行走天下的勇气"的产品初衷 | `backend/app/planner.py`、`backend/app/models.py`、`frontend/src/views/Plan.vue`、`frontend/src/components/DayCard.vue` |
| 2026-08-28 | 新功能 | **周边探索**：新增探索页（路由 `/explore`，首页加入口）。在地图上点击任意位置设为中心点，后端 `/api/explore` 以该点为中心（可调 3/5/10/20km 半径、景点/美食/购物/夜生活类别）检索周边 POI 并返回距中心距离，前端**放射状可视化**（中心红点 + 中心→各点位虚线 + 类别色圆点/名称标签），底部点位列表（评分/距离/区域）点击聚焦；高德/SVG 双模式、POI 结果接入 TTL 缓存。浏览器实测：选点 → 25 个点位放射渲染、参数联动正常 | 新增 `frontend/src/views/Explore.vue`；`backend/app/amap.py`、`backend/app/models.py`、`backend/app/main.py`、`frontend/src/api/index.js`、`frontend/src/router/index.js`、`frontend/src/views/Home.vue`、`README.md` |
| 2026-08-28 | 性能优化 | 高德调用全链路 **TTL 缓存**：地理编码 7d / POI·行政枚举·地标解析·周边搜索 1d / 路径 30m / 天气 2h；失败不缓存、读取深拷贝防污染、进程内存（重启即清）；**精简检索关键词**（景点关键词去"纪念馆"、行政枚举去"纪念馆/森林公园"）；`/api/status` 增加缓存占用监控。实测同场景规划耗时 **~10s → 0.3s**（快约 30 倍），第二次规划几乎 0 额外高德调用 | 新增 `backend/app/cache.py`；`backend/app/amap.py`、`backend/app/planner.py`、`backend/app/main.py` |
| 2026-08-28 | 规划引擎 | 多城市实测验证发现并修复**省级枚举跨城污染**（佛山被规划出广州塔/惠州西湖）：POI 增加 `cityname` 字段，行政枚举按城市归属过滤不属于目标城市的点位。修复后佛山只出本地（禅城/南海/顺德三区），广州不再出惠州西湖，杭州"乌镇"正确剔除（属嘉兴）改出真正的千岛湖 | `backend/app/amap.py`、`backend/app/planner.py` |
| 2026-08-27 | 规划引擎 | **对标竞品优化 A+B**：①`must_visit` 必打卡地标库（`data/must_visit.py`）正式接入主线景点池——此前只注入辅助池、从未进主线，导致北京规划不出长城/颐和园；已在行政枚举里的同名地标补打 must_visit 权重。②地标排序加权 `_rank()`（must_visit +0.3 / hot +0.1，仅排序不改展示分）。③修复"天安门被挤出"：`_greedy_group` 多个顶级地标同分时优先离市中心近的，使故宫/天安门/国博紧凑成组。④`_dedup_landmark_day` 同一天命中同一地标词（如"长城"）只留评分最高，避免"长城三连"。实测北京 3 天 = 国博·天安门·故宫 / 颐和园·圆明园·香山 / 八达岭长城 | `backend/app/planner.py` |
| 2026-08-27 | 地图前端 | **路线渲染修复**：高德 `BezierCurve` 在实际页面不绘制 → 改为"Catmull-Rom 转三次贝塞尔采样成密集折点 + 普通 `Polyline`"渲染，任何高德版本稳定绘制且保持柔和曲线 | `frontend/src/components/MapView.vue` |
| 2026-08-27 | 地图前端 | **卡片 ↔ 地图联动 + 线点优化**：点击行程卡片地图 `setCenter` 聚焦点位并放大（zoom≥14）+ 高亮红点 + 滚动到地图；marker 改自定义圆点 + `anchor:'center'` 修复"线与点错开"；路线改贝塞尔平滑、降低透明度、不同天不同颜色（线点同色） | `frontend/src/components/MapView.vue`、`DayCard.vue`、`views/Plan.vue` |
| 2026-08-27 | 规划引擎 | **路径规划规则调优**：①跨天地域不重复（第1天杭州/第2天绍兴不再混杭州点位）；②行政分级判断目的地等级并枚举区县/市/省景点，美食/购物/夜生活只做就近辅助不入主线；③出行距离短的点位优先同一天；④"由近及远、按圈层轮流"分配天序（市区/近郊/远郊），并修复"天安门不在规划"（地标优先 + 评分加权） | `backend/app/planner.py` |
| 2026-08-27 | 规划引擎 | **景点推荐优化（方向1）**：解决"返回结果全是美食"——POI 类型推断 `_infer_category`（防止小模型把蜜雪冰城标成夜生活）、评分/热度排序、美食辅助点名称护栏、每日品类均衡（美食 ≤2/天、补足缺失品类） | `backend/app/amap.py`、`backend/app/planner.py` |
| 2026-08-27 | 数据真实 | **真实数据落地**：后端配置 Web服务 Key 并启动时真实探测（`probe_amap`），Key 不可用则诚实降级 mock；内置 `data/place_geo.json`（全国 363 地级市 + 3147 区县归属 + 112 精确区县坐标）修复"茌平定位错误"等任意地名定位 | `backend/app/amap.py`、`backend/app/config.py`、`backend/app/data/place_geo.json` |
| 2026-08-27 | 前端功能 | **对话式规划**：新增 ChatPlanner 组件，支持自然语言对话生成行程（多轮调整、缺目的地主动追问），与手动表单双模式 | `frontend/src/components/ChatPlanner.vue`、`backend/app/intent.py`、`backend/app/main.py` |
| 2026-08-27 | 项目搭建 | 初始版本：Vue3+Vite 前端（H5）、Python FastAPI 后端、本地轻量模型（Ollama qwen2.5:3b）接入、高德 Web服务 API（数据）+ 高德 JS API（地图渲染）、规则引擎兜底 + AI 编排 + 时间轴校验 + 预算拆解 + 地图可视化 + 拖拽重排 | 全项目 |

## API 一览

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/api/status` | 运行状态（高德是否真实模式、AI 模型是否可用、缓存占用） |
| POST | `/api/plan` | 一键生成行程（核心接口） |
| POST | `/api/chat/plan` | 对话式规划（解析自然语言 → 生成行程） |
| POST | `/api/route` | 两点间交通方案（拖拽重排用） |
| POST | `/api/poi/search` | 按城市/类别检索 POI |
| POST | `/api/explore` | 周边探索：以坐标为中心检索周边 POI（含距中心距离，放射状可视化用） |
| POST | `/api/auth/register` | 注册（bcrypt 密码哈希；用户名 2-20 位、密码 4-64 位） |
| POST | `/api/auth/login` | 登录，返回 JWT（HS256，默认 24h 过期）+ 用户信息；旧 sha256 密码自动升级 bcrypt |
| GET | `/api/auth/me` | 校验 JWT，用于刷新页面后恢复登录态；过期/无效返回 401 |
| POST | `/api/trips` | 登录用户上报一次出行；未登录静默忽略（不记录） |
| GET | `/api/trips` | 查询本人出行记录与足迹汇总；未登录返回 401 |

### POST /api/chat/plan 请求示例

```json
{
  "messages": [
    { "role": "user", "content": "和对象去上海2天，经济一点，喜欢夜景和购物" },
    { "role": "assistant", "content": "已为你生成方案…" },
    { "role": "user", "content": "预算再省一点" }
  ]
}
```

> 返回 `ready`（是否已生成）、`reply`（助手回复文案）、`params`（解析出的参数）、`plan`（完整行程，`ready=true` 时存在）。目的地缺失时返回追问文案，`ready=false`。

### POST /api/plan 请求示例

```json
{
  "destination": "杭州",
  "days": 3,
  "group_type": "情侣",
  "budget_level": "适中",
  "style": "美食+人文",
  "pace": "适中",
  "traffic_mode": "混合",
  "daily_start_time": "09:00",
  "daily_end_time": "21:00",
  "must_include_poi": ["西湖风景区"],
  "exclude_poi": [],
  "interests": ["夜景", "美食"]
}
```

## 相关文档

| 文档 | 说明 |
|---|---|
| [`docs/code-structure.md`](docs/code-structure.md) | 代码目录结构梳理（后端/前端模块职责、调用关系、常用命令） |
| [`docs/planner-rules.md`](docs/planner-rules.md) | 景点规划引擎规则梳理（主流程、行政分级枚举、聚类、逐日构建、跨天去重、参数速查） |
| [`docs/rag-knowledge-base-plan.md`](docs/rag-knowledge-base-plan.md) | 景点人文RAG知识库建设与落地计划（知识库结构、技术选型、四大应用场景、分阶段落地、数据库设计、API设计） |

## 常见问题

- **生成特别慢？** 正在使用 7B 等大模型做 CPU 推理（一次约 1-2 分钟）。拉取 `qwen2.5:3b` 后默认走轻量模型，速度显著提升。
- **接口报 500 / 页面空白？** 确认后端 `run.py` 已启动（`http://127.0.0.1:8000/docs` 可访问）。
- **如何切回真实高德数据？** 在 `backend/.env` 填入 `AMAP_KEY` 后重启后端；状态栏会显示"高德·真实数据"。
